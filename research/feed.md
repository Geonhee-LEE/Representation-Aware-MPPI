# Research Feed

Auto-populated by `scripts/researcher.sh` (4-hourly cron). Newest first, cap 30
entries. Per-cycle archive lives under `research/YYYY-MM/<seq>.md`. Format and
conventions: see [`research/README.md`](README.md).

---

## 2026-07-11 17:00 — CBFKit: JAX CBF toolbox (Toyota Research) — user 지정 reference
- **Source**: https://github.com/bardhh/cbfkit (arXiv 2404.07158, 99⭐, **BSD-3-Clause 명시**)
- **Type**: github
- **Why relevant**: user 가 직접 지정한 S1 (issue #65) reference. planner(MPPI)→nominal→**safety filter(CBF-QP)** 순수함수 파이프라인 + unicycle CBF 예제군 (pedestrian head-on = 우리 S02 그대로) + **stochastic/robust/risk-aware CVaR-CBF** (P3 aleatoric δ/CVaR 라우팅 D-014 와 정확히 겹침) + Neural CBF (learned h(x) — representation 가설의 CBF-side 대응물) + fast QP solver (2×5 에서 880× vs JAXopt). safe_control 대비 차별점: 라이선스 명시(BSD-3), JAX vmap/jit, MPPI 구현 내장, 시그니처가 순수함수 계약으로 문서화 — sandbox controller plug-in 계약과 1:1 대응. Borrow: (a) **safety-filter 아키텍처** — sandbox `cbf_mppi` = MPPI nominal + CBF-QP projection (구현 완료, 이 cycle), (b) risk-aware CVaR-CBF 수식 → D-014 AleatoricRiskCritic 검증 기준, (c) Neural CBF → P3 학습 representation 의 안전 인증 경로 후보.
- **Suggested TODO**: [research] cbfkit stochastic-CBF (SDE) 와 우리 epistemic k·σ margin (D-013) 의 이론적 관계 정리 — σ(x)dw 항 CBF 조건이 margin inflation 의 원리적 상한인지

## 2026-07-11 16:00 — EgoWalk: A Multimodal Dataset for Robot Navigation in the Wild
- **Source**: https://arxiv.org/abs/2505.21282
- **Type**: dataset
- **Why relevant**: Timur Akhtyamov, Mohamad Al Mdfaa, Javier Antonio Ramirez Benavides et al. (v1 May 27 2025, **v2 Apr 19 2026**; CC-BY-4.0; data + processing pipelines openly released). **The *human-demonstration navigation corpus* the feed's human-demo-dependent arms have been missing — 50 hours of multimodal human navigation recordings across diverse indoor/outdoor environments and multiple seasons, released in both raw and imitation-learning-ready formats, with explicit support for traversability segmentation and language-goal annotation.** Why this matters structurally: three recent feed entries assume a human-demonstration dataset the project does not yet have — Semantic-Metric Bayesian Risk Fields [[2512.08233]] trains its ViT risk mapping *from human demonstration videos*; Crowd-FM [[2602.06698]] learns its human-likeness trajectory selector *from human demonstrations*; and the P3 traversability channel needs labeled traversability data for café/small_city-like venues. EgoWalk is a drop-in candidate for all three: 50 h of egocentric human walking covers exactly the "where do humans choose to walk" signal the Bayesian risk field's demo-distribution requirement demands, the traversability segmentation task is natively supported (BEV odometry projected onto images as footstep-location pseudo-labels), and indoor/outdoor multi-season coverage spans the café→small_city venue axis. As a dataset (not a method) it carries no planner-architecture commitment — it feeds the representation side of the classical-planner + learned-representation thesis directly. North-star fit: **P3 (traversability channel training data + the human-demo corpus for the [[2512.08233]] VLM-prior risk field) + P4/P5 (human-demo source for the Crowd-FM [[2602.06698]] human-likeness selector and social-quality scoring) + 미관측-distribution clause (in-the-wild, multi-season, multi-venue recordings as an OOD-adjacent training/eval distribution)**. Caveats: **egocentric human walking, not robot-embodied** — viewpoint height, dynamics, and camera geometry differ from a diff-drive robot's sensor suite (domain gap must be measured, e.g. via a small robot-collected calibration set); submitted to IEEE but still a preprint; 50 h is modest by foundation-model standards (fine for fine-tuning/ViT-head training, not for pretraining); no MPPI/planner component at all — it's purely a data asset. Borrow is the **dataset itself as the human-demo training corpus for the P3 risk-field/traversability heads** — verify license/pipeline usability, then train the [[2512.08233]] ViT risk head on EgoWalk instead of collecting café/small_city human demos from scratch.
- **Suggested TODO**: evaluate **EgoWalk (CC-BY-4.0, 50 h human navigation)** as the human-demo training corpus the P3 pipeline needs — pull the traversability-segmentation split, check domain gap vs the café/small_city robot viewpoint, and test whether the Semantic-Metric Bayesian Risk Field [[2512.08233]] ViT risk head and the Crowd-FM [[2602.06698]] human-likeness selector can train on it without venue-specific demo collection.

## 2026-07-11 16:00 — RaWMPC: Risk-Aware World Model Predictive Control for Generalizable End-to-End Autonomous Driving
- **Source**: https://arxiv.org/abs/2602.23259
- **Type**: arxiv
- **Why relevant**: Jiangxin Sun, Feng Xue, Teng Long, Chang Liu, Jian-Fang Hu, Wei-Shi Zheng, Nicu Sebe (v1 Feb 26 2026; cs.CV; autonomous driving). **The *risk-aware interaction-data-collection* arm for P2 dynamics training — an end-to-end world-model MPC that (1) predicts consequences of multiple candidate actions with a learned world model, (2) selects low-risk actions via explicit risk evaluation over those predicted futures, and (3) trains *without expert demonstrations* by deliberately exposing the model to hazardous behaviors during interaction, then distilling risk-avoidance into an action proposal network.** Structurally this is MPPI-with-learned-dynamics by another name — sample candidate actions, roll out through a learned model, score by risk — which is exactly the project's P2/P3 architecture, so its claims are a useful external validation that explicit risk evaluation over learned-model rollouts generalizes better OOD than demonstration-cloned policies (reported to outperform SOTA **in both in-distribution and out-of-distribution scenarios**, the 미관측 clause). The genuinely borrowable piece is the **risk-aware interaction strategy for training-data coverage**: a learned dynamics/risk model trained only on safe teleop or nominal navigation data never sees near-collision states, so its epistemic σ (P3) is untrustworthy exactly where the planner needs it most; RaWMPC's recipe — deliberately collect hazardous-interaction episodes and distill — is a concrete answer to the P2 question "what data does the residual-dynamics ensemble need to be calibrated near obstacles." The distillation of rollout-search into an action proposal network is also structurally parallel to the feed's proposal-learning arms (GPC [[2510.14643]] flow-matching warm-start): world-model search amortized into a proposal prior. North-star fit: **P2 (hazardous-state data-collection strategy for the learned-dynamics ensemble) + P3 (risk evaluation over learned-model rollouts, epistemic coverage near obstacles) + 미관측-distribution clause (OOD generalization without expert demos)**. Caveats: **end-to-end (not modular, contrary to the classical-planner + learned-representation thesis — borrow is strictly the data-collection strategy and the risk-evaluation framing, not the architecture)**; cs.CV autonomous-driving domain, no diff-drive/Nav2/Gazebo; **no code surfaced**; evaluation platform/simulator unstated in the abstract; no quantitative numbers in the abstract; not MPPI-native. Borrow is the **risk-aware interaction data-collection strategy** — when collecting Gazebo training data for the P2 residual-dynamics ensemble, add deliberately hazardous near-obstacle episodes (aggressive approach, late braking) so the ensemble's epistemic σ is calibrated in the near-collision regime the P3 risk field queries.
- **Suggested TODO**: <none — file as the *risk-aware interaction data-collection* concept for P2: when the Gazebo training-data collection for the residual-dynamics ensemble is designed, include deliberately hazardous near-obstacle episodes (RaWMPC recipe) so epistemic σ is calibrated near collision states; end-to-end AD paper, no code, concept-only borrow.>

## 2026-07-10 20:00 — Towards Generalizable Safety in Crowd Navigation via Conformal Prediction
- **Source**: https://arxiv.org/abs/2508.05634
- **Type**: arxiv
- **Why relevant**: Jianpeng Yao, Xiaopan Zhang, Yu Xia, Zejin Wang, Amit K. Roy-Chowdhury, Jiachen Li (v1 Aug 7 2025; cs.RO; **CoRL 2025**; simulation + real-robot deployment). **The *adaptive-conformal-inference + constrained-RL* arm for OOD-robust crowd navigation safety — the first feed entry that applies adaptive conformal inference *upstream at the pedestrian predictor output* and uses the resulting coverage-calibrated uncertainty estimates to constrain an online policy under distribution shift in crowd scenarios.** Mechanism: pedestrian trajectory predictions are wrapped with **adaptive conformal inference** that generates coverage-guaranteed uncertainty bounds, adjusting thresholds online as crowd behavior shifts; those uncertainty estimates guide a **constrained RL policy** that adapts its safe avoidance behavior using the CP uncertainty signal — reported **96.93% in-distribution success rate (+8.80% over baselines)**, **3.72× fewer collisions**, **2.43× fewer trajectory intrusions**, with demonstrated robustness to velocity variations, pedestrian policy changes, and transitions from individual to group dynamics. The adaptive CP mechanism is the borrowable piece for P4: as a coverage-calibration wrapper around any pedestrian predictor feeding the dynamic risk channel, it provides OOD-robust coverage guarantees without distributional assumptions — applicable when the nav2_mppi DRA-MPPI [[2506.21205]]-style MC rollout selection uses pedestrian predictions whose confidence shifts across venues (café → small_city → 미관측 rosbag). Companion to Rethinking-Gaussian [[2603.10407]] (parametric covariance recalibration), Scenario-aware UQ [[2512.05682]] (CopulaCPTS temporal intervals), and When-Environments-Shift [[2602.12616]] (robust CP under bounded distribution shift): this adds **adaptive online threshold updating** as the CP-calibration mechanism most suited to time-varying crowd distribution shifts during deployment. **Code available** at gen-safe-nav.github.io. North-star fit: **P4 (crowd navigation under distribution shift — OOD robustness to group dynamics, velocity variations, and policy changes, CoRL 2025, real robot) + the pedestrian-prediction calibration thread (adaptive CP as the distribution-free, online-adapting member) + the 미관측-distribution-shift clause (adaptive thresholds maintain coverage as the crowd model shifts)**. Caveats: **constrained RL policy, not MPPI/sampling-based MPC — borrow is strictly the adaptive conformal inference wrapper around the pedestrian predictor (coverage-calibrated prediction uncertainty as the P4 risk signal), not the RL policy**; robot morphology and ROS stack unspecified; **code confirmed available** at gen-safe-nav.github.io but interface (standalone module vs RL-policy-integrated) needs verification before nav2_mppi integration. Borrow is the **adaptive conformal inference calibration wrapper as a coverage-guaranteed, OOD-robust pedestrian-prediction uncertainty generator** for the P4 dynamic risk channel — evaluate whether adaptive-threshold CP covers more collision events during distribution-shifted crowd scenarios than Rethinking-Gaussian [[2603.10407]] parametric recalibration and DUCCT-MPPI [[2605.28330]] MC aggregation.
- **Suggested TODO**: prototype **adaptive conformal inference as the pedestrian-prediction calibration wrapper** (2508.05634 recipe) for nav2_mppi's P4 dynamic risk channel — wrap the pedestrian predictor in online-adaptive CP thresholds, evaluate coverage robustness under crowd distribution shifts (velocity/policy/group-dynamics variations, the exact OOD axes CoRL 2025 benchmarks), A/B vs Rethinking-Gaussian [[2603.10407]] parametric recalibration on a cafe crossing → small_city → OOD shift; code at gen-safe-nav.github.io.

## 2026-07-10 20:00 — Semantic-Metric Bayesian Risk Fields: Learning Robot Safety from Human Videos
- **Source**: https://arxiv.org/abs/2512.08233
- **Type**: arxiv
- **Why relevant**: Timothy Chen, Marcus Dominguez-Kuhne, Aiden Swann, Xu Liu, Mac Schwager (Dec 9 2025; cs.RO; **code available** at riskbayesian.github.io). **The *VLM-prior + human-video-learned Bayesian risk field* arm for P3 — produces continuous, spatially-varying, object-class-conditioned risk maps from RGB images trained on human demonstration videos, where the VLM prior enables generalization to novel/unexpected object classes without explicit risk annotation.** Mechanism: a **pretrained VLM supplies a semantic prior** over what kinds of objects are dangerous in what contexts; a **learned ViT maps VLM pretrained features to pixel-aligned spatially-varying risk values**; the Bayesian fusion (prior + ViT) yields a continuous risk field conditioned on what the object is (VLM) and human-demonstrated safety context (demonstration videos), enabling generalization to unseen object classes without explicit "danger" labeling. Structurally distinct from the feed's P3 risk entries: geometric-SDF risk (EXACT-MPPI [[2605.29663]]) hardens obstacle geometry without semantics; conformalized-distance risk (FCP-MPC [[2607.00776]]) calibrates the distance field without conditioning on object class; DUCCT-MPPI [[2605.28330]] propagates localization + prediction uncertainty without VLM semantic grounding. This is the **semantically-conditioned, VLM-grounded risk field** arm — the object-class-aware risk channel the north star's "의외" (unexpected class) clause demands: a closed-set semseg backbone assigns no risk to OOV objects; this VLM-prior field assigns a semantics-aware conservative risk even to unseen classes. Companion to OVBEVSeg [[2606.24353]] (open-vocabulary BEV *segmentation*): OVBEVSeg answers "what is it" in BEV; this answers "how dangerous is it" in image space — combining them gives a semantically-aware, class-conditioned risk field the planner can query for unexpected obstacles. **Code available** at riskbayesian.github.io. North-star fit: **P3 (VLM-prior Bayesian risk field as the semantics-conditioned risk channel) + north-star "의외" class completeness (novel-object risk generalization via VLM prior) + the OVBEVSeg [[2606.24353]] pairing (segmentation → what is it; risk field → how dangerous)**. Caveats: **arxiv preprint only — no robot/simulator platform confirmed in abstract**; the ViT risk mapping is learned from human demo videos (demo distribution must overlap with deployment venues — café/small_city human demos needed); no quantitative collision/near-miss numbers in abstract; Bayesian fusion may miscalibrate when VLM semantic prior is far from deployment domain; **BEV lifting not provided** (image-space risk field must be projected into BEV for nav2_mppi critic — integration step). Borrow is the **VLM-prior + ViT pixel-aligned risk field as a P3 semantically-conditioned risk channel** — prototype on the café environment with novel unexpected objects, score risk prediction vs closed-set semseg costmap and vs OVBEVSeg [[2606.24353]] open-vocabulary BEV segmentation front-end; code available.
- **Suggested TODO**: prototype **Semantic-Metric Bayesian Risk Field** (2512.08233 recipe) as a P3 risk channel for nav2_mppi — collect human-demo navigation videos in café/small_city environments, train ViT risk mapping with VLM prior, project pixel-aligned risk field into BEV, evaluate novel/unexpected object risk generalization vs closed-set semseg costmap and vs OVBEVSeg [[2606.24353]] open-vocabulary segmentation on a scene with out-of-vocabulary obstacles; code at riskbayesian.github.io.

## 2026-07-09 20:00 — MPPI-DK: Accelerating Sampling-Based Control via Learned Linear Koopman Dynamics
- **Source**: https://arxiv.org/abs/2603.05385
- **Type**: arxiv
- **Why relevant**: Wenjian Hao, Yuxuan Fang, Zehui Lu, Shaoshuai Mou (v1 Mar 5 2026; cs.RO; arxiv preprint). **The *learned-linear-Koopman-dynamics* arm of the P2 rollout-acceleration thread — substitutes the nonlinear dynamics in MPPI with a deep Koopman operator (DKO) that linearizes the state space in a learned latent, enabling fast analytical k-step trajectory propagation learned directly from interaction data (no analytical model required).** Mechanism (MPPI-DK): a deep neural network learns a Koopman lift to a higher-dimensional linear latent; MPPI rollouts are propagated through the linear surrogate (matrix power `Aᵏ x₀`) instead of a nonlinear ODE solver per step, cutting per-rollout compute; evaluated on pendulum (sim) + **surface vehicle navigation** (sim) + reference-tracking on a **quadruped** (hardware). The speed argument: where ICODE-MPPI [[2605.03260]] integrates a neural-ODE residual per step (high fidelity, higher cost) and Variational Neural Dynamics [[2606.27353]] runs a recurrent condition-encoder (multi-venue adaptation, heavier), MPPI-DK is the *compute-speed arm* — linear propagation is the natural compute lever when the P1/P3 BEV + risk-field front-end is expensive and per-rollout budget is tight. The interaction-data learning loop (no analytical model required) is compatible with the nav2_mppi + Gazebo diff-drive training loop, and the surface-vehicle navigation evaluation is closer to diff-drive mobile robot nav than the manipulator papers. North-star fit: **P2 (fast learned-dynamics surrogate for MPPI rollouts when the BEV/risk-field front-end is expensive) + 경로추종 (more rollouts at fixed compute = better trajectory optimization)**. Caveats: **arxiv preprint, no conference; no diff-drive/Nav2/Gazebo confirmed — surface vehicle (sim) + quadruped (hardware); Koopman linearity degrades in highly cluttered/obstacle-dense environments where nonlinear dynamics dominate; point-dynamics output only — no epistemic σ without adding a variance head; no code/project page surfaced; no head-to-head vs ICODE-MPPI [[2605.03260]] / Variational Neural Dynamics [[2606.27353]] on nav tasks**. Borrow is the **deep Koopman operator as a fast linear rollout-dynamics surrogate for nav2_mppi** — learn a DKO from Gazebo diff-drive interaction data, measure rollout speedup vs trajectory quality (cross-track error, near-miss) at fixed sample budget, A/B vs ICODE-MPPI [[2605.03260]] ODE-residual on cafe-straight, checking whether Koopman linearity holds in the cluttered cafe corner.
- **Suggested TODO**: prototype **MPPI-DK (deep Koopman operator dynamics)** as a fast rollout-dynamics substitute in nav2_mppi — learn a DKO from Gazebo diff-drive interaction data, measure rollout speedup vs quality (cross-track error, near-miss rate) at fixed sample budget, A/B vs ICODE-MPPI [[2605.03260]] ODE-residual on cafe-straight; check whether Koopman linearity holds in a cluttered cafe corner (the Koopman-linearity failure case).

## 2026-07-09 16:00 — Open-Vocabulary BEV Segmentation with 3D-Aware Geometric Constraints (OVBEVSeg)
- **Source**: https://arxiv.org/abs/2606.24353
- **Type**: arxiv
- **Why relevant**: Hojun Choi, Seulbin Hwang, Dae Jung Kim, Kisung Kim, Hyunjung Shim, Jinhan Lee (v1 Jun 23 2026; cs.CV; **ECCV 2026**). **The *open-vocabulary BEV perception* arm for the P1 BEV representation front-end — addresses the "의외" (unexpected object class) axis in the north star by enabling BEV semantic segmentation to generalize beyond the training category set using vision-language models (VLMs), solving the 3D geometric inconsistency in 2D→BEV lifting via Gaussian-splatting-based unprojection.** Mechanism (OVBEVSeg): (1) pseudo-labeling via 3D VLM projection to generate open-vocabulary BEV labels without novel-class annotation; (2) joint 2D-BEV optimization with 3D-aware structural constraints for geometric consistency; (3) geometric distillation for efficient real-time deployment. **Results: +15.3 mIoU over closed-set methods on unseen categories; 2.5× faster inference with 0.22× memory vs projection-based methods; competitive with semi-supervised baselines at 40% annotation budget with zero novel-class labels. ECCV 2026.** The north star's "의외" clause ("unexpected" object classes — baby stroller, spilled cart, crowd barrier) is exactly the open-vocabulary gap: a closed-set BEV segmentation model trained on standard categories produces no semantic signal for out-of-vocabulary objects, while OVBEVSeg's VLM backbone recognizes them zero-shot. The 3D-aware Gaussian-splatting-based geometric distillation addresses the P1 implementation challenge of lifting monocular/perspective images into a geometrically consistent BEV grid the cost function or risk channel can query. The 2.5× speed + 0.22× memory advantage makes real-time deployment at the nav2_mppi critic rate feasible. For P3: the open-vocabulary semantic map is the conditioning representation layer on which risk/uncertainty channels condition — unknown-category objects that produce a semantic label can be assigned a conservative risk prior rather than being silently ignored. North-star fit: **P1 (open-vocabulary BEV representation front-end — extends beyond closed-set pretrained semseg to handle unseen/unexpected object classes in cafe + small_city + 미관측-rosbag) + P3 (open-vocabulary semantics as the conditioning input to risk/uncertainty channels) + north-star "의외 class" completeness clause**. Caveats: **platform is autonomous driving BEV benchmark — no diff-drive / Nav2 / Gazebo confirmed**; VLM backbone (likely CLIP/SAM) may need fine-tuning for indoor robotic environments; Gaussian-splatting unprojection may require calibrated depth/multi-view input (monocular fallback unconfirmed); **no public code** (project page cited, release status unconfirmed); the +15.3 mIoU is on an autonomous driving benchmark, not a robot nav task; ECCV 2026 is past the project's current P4 window. Borrow is the **VLM + 3D-aware Gaussian-splatting-unprojection recipe as an open-vocabulary BEV semantic layer for the P1 front-end** — replace the closed-set pretrained semseg backbone with VLM-driven open-vocabulary BEV segmentation, score zero-shot "의외" object recognition on a cafe scene with out-of-vocabulary objects, and condition P3 risk channels on the resulting open-vocabulary semantic map.
- **Suggested TODO**: prototype the **OVBEVSeg open-vocabulary BEV semantic layer** as the P1 representation front-end — replace the closed-set pretrained semseg backbone with VLM + Gaussian-splatting-based unprojection (2606.24353 recipe), evaluate zero-shot "의외" object recognition on a cafe scene containing out-of-vocabulary objects, and condition P3 risk/uncertainty channels on the resulting open-vocabulary semantic map; ECCV 2026, autonomous-driving benchmark, no Nav2/code confirmed.

## 2026-07-09 20:00 — Crowd-FM: Learned Optimal Selection of Conditional Flow Matching-Generated Trajectories for Crowd Navigation
- **Source**: https://arxiv.org/abs/2602.06698
- **Type**: arxiv
- **Why relevant**: Antareep Singha, Laksh Nanwani et al. (v1 Feb 6 2026, v2 Mar 17 2026; cs.RO; **ICRA 2026**). **The *CFM-trajectory-bank-generation + human-likeness-selection* arm for crowd navigation — fills a gap in the generative-proposal thread the GPC [[2510.14643]] entry contextually named ("Crowd-FM adds a learned human-likeness *selector*") but never gave a dedicated treatment.** Mechanism: (1) trains a **Conditional Flow Matching policy** on a dataset of optimally controlled trajectories to generate a bank of candidate collision-free navigation trajectories; (2) learns a **scoring function from human demonstrations** to select the most human-like trajectory from the CFM-generated bank — coupling generative trajectory generation with a human-preference selector. **Accepted at ICRA 2026.** Surpasses existing learning-based approaches and can match optimization-based planning on crowd navigation. Structural placement in the generative-proposal thread: CFM-MPPI [[2508.01192]] refines a CFM-generated bank with MPPI (iterative refinement); GPC [[2510.14643]] trains a flow-matching model on the sampler's own rollouts and warm-starts the sampler (self-bootstrapping); Crowd-FM generates a bank via CFM and selects by learned human-likeness (bank-generation + learned-selection) — three poles of the generative-trajectory-for-navigation space are now distinct. The human-likeness scoring function is the borrowable piece for P4: instead of selecting the "lowest-cost" trajectory from a nav2_mppi rollout batch, select by a *learned human-likeness/social-comfort score* — exactly what the P4 dynamic-risk channel needs to go beyond collision avoidance to social-space comfort (near-miss, blockage, perceived naturalness). Natural companion to iCrowdNav [[2606.26047]]'s I²Former intention predictor: iCrowdNav encodes *where pedestrians are going*; Crowd-FM's selector decides *which robot trajectory is human-preferred given that context*. North-star fit: **P4 (crowd navigation, ICRA 2026) + the generative-trajectory-proposal thread (CFM bank + human-likeness selection arm) + P5 (social-quality metric: human-likeness score as a complement to near-miss/collision rate)**. Caveats: **platform/simulator unstated in the abstract — no diff-drive / Nav2 / Gazebo confirmed**; not MPPI-native (CFM + learned scorer, not a sampler — borrow is the *human-likeness scoring function + CFM-generated trajectory bank concept*, not the full CFM policy loop); the human-preference labels require a demonstration-labelling pipeline; **no code confirmed**. Borrow is the **learned human-likeness scoring function as a social-quality trajectory-selection head** — add it on top of nav2_mppi's rollout batch, evaluate on a cafe crossing scene scoring near-miss / blockage / perceived naturalness alongside collision rate, A/B vs minimum-collision-cost selection and vs the I²Former-intention-weighted risk field (iCrowdNav [[2606.26047]]) on the same scene.
- **Suggested TODO**: prototype a **learned human-likeness trajectory selector** (Crowd-FM recipe) as a social-quality head on nav2_mppi's rollout batch — train a scoring function from human-demonstration data that re-ranks rollouts by social comfort, add near-miss / blockage / perceived naturalness as P5 harness social-quality axes, and A/B vs minimum-collision-cost selection and vs iCrowdNav [[2606.26047]] I²Former-intention-weighted risk field on a cafe crossing scene; ICRA 2026, CFM-based crowd navigation, platform/code unconfirmed.

## 2026-07-09 16:00 — When Environments Shift: Safe Planning with Generative Priors and Robust Conformal Prediction
- **Source**: https://arxiv.org/abs/2602.12616
- **Type**: arxiv
- **Why relevant**: Kaizer Rahaman, Jyotirmoy V. Deshmukh, Ashish R. Hota, Lars Lindemann (v1 Feb 13 2026; eess.SY; ORCA pedestrian simulator). **The *distribution-shift-robust conformal prediction* arm of the safety-guarantee thread — the first feed entry that specifically maintains CP safety guarantees when the deployment environment differs from the calibration distribution, combining conditional diffusion models with robust conformal prediction inside MPC.** Mechanism: a **conditional diffusion model generates synthetic calibration data** reflecting observed environmental parameters (traffic density, pedestrian density), and a **robust conformal prediction** procedure calibrates coverage guarantees that remain valid under bounded distribution shifts — embedded inside an MPC for collision-free navigation among dynamic agents. This fills a genuine gap: every other CP entry in the feed (FCP-MPC [[2607.00776]], OCULAR [[2605.13028]], CSRC [[2606.04185]], Pixels-to-Proofs [[2606.15594]], DRA-MPPI [[2506.21205]], Scenario-aware UQ [[2512.05682]]) assumes i.i.d. deployment vs calibration distribution — none explicitly provides robustness guarantees when that assumption breaks. For the project's 미관측-distribution (real-world rosbag) stage this is the structural answer: "robust CP" maintains a coverage guarantee under a bounded shift between the calibration distribution and the deployment environment, without requiring i.i.d. deployment data. The conditional diffusion model as a synthetic calibration data generator is also novel: instead of requiring real held-out calibration episodes from the deployment environment, the diffusion model synthesizes calibration data conditioned on observed context parameters, a practical solution when real deployment data is scarce. North-star fit: **P3 (robust CP safety guarantees for the risk field under distribution shift) + the 미관측-rosbag stage (robust CP designed for deployment-time distribution mismatch — the structural gap all i.i.d. CP entries leave open) + P5 (robust CP coverage as a guarantee that survives venue-to-venue shifts, the strictest calibration criterion for multi-venue evaluation)**. Caveats: **ORCA pedestrian simulator, autonomous vehicles — no diff-drive / Nav2 / Gazebo confirmed**; standard MPC, not MPPI-native (borrow is strictly the *robust CP + diffusion calibration-data-generation concept*, not the MPC machinery); **bounded-distribution-shift assumption must be quantified** (the shift-magnitude bound is a hyperparameter robust CP needs, and calibrating it for café→real-world shift is an open question); **no code surfaced**; diffusion model for calibration data generation adds training cost. Borrow is strictly the **robust conformal prediction under bounded distribution shifts + conditional diffusion model for synthetic calibration data generation** as the P3/P5 distribution-shift-robust CP recipe — prototype robust CP as the calibration procedure for the 미관측-rosbag stage where i.i.d. calibration fails, evaluated against OCULAR [[2605.13028]]'s visually-similar-environment transfer and FCP-MPC's [[2607.00776]] AFCP online adaptation on a café→small_city→rosbag distribution shift.
- **Suggested TODO**: when the P5 σ-calibration harness reaches the 미관측-rosbag distribution-shift stage, prototype **robust conformal prediction** (2602.12616 recipe) as the calibration procedure — compare its bounded-distribution-shift guarantee against the i.i.d. CP baselines (OCULAR [[2605.13028]] visually-similar-transfer, FCP-MPC [[2607.00776]] AFCP adaptation) on a café-to-real-world venue shift; the conditional diffusion model as a synthetic calibration-data generator is the companion concept for venues where real held-out deployment data is unavailable.

## 2026-07-09 08:00 — Tempered Sequential Monte Carlo for Trajectory and Policy Optimization with Differentiable Dynamics
- **Source**: https://arxiv.org/abs/2604.21456
- **Type**: arxiv
- **Why relevant**: Heng Yang (RSS 2026; v1 Apr 23 2026, v2 May 9 2026; cs.RO). **The *adaptive-tempering / HMC-particle-diversity* arm of the sampler-theory thread — treats trajectory/policy optimization as probabilistic inference over a KL-regularized cost distribution and proposes tempered sequential Monte Carlo (TSMC) with Hamiltonian MC (HMC) rejuvenation to anneal particles from prior to posterior while maintaining diversity, addressing the mode-collapse failure that MPPI importance weighting faces in steep/multimodal landscapes.** Mechanism: a tempering path progressively shifts particles from the prior to the cost-weighted posterior; at each tempering step, importance reweighting + resampling selects high-quality particles, and **HMC with dynamics gradients rejuvenates particles** to prevent collapse. Extensions handle policy optimization via deterministic approximation of initial-state distributions and stochastic rollouts. **RSS 2026 (peer-reviewed, prestigious robotics venue).** This is the third arm of the sampler-core foundations thread (EM-MPPI [[2606.00317]] as the E-M view; preconditioned-GD [[2603.24489]] as the unit-step-geometry view) — but where those papers characterize *what* the MPPI update is, TSMC proposes *how to improve it*: adaptive tempering replaces fixed temperature λ with a schedule, and HMC rejuvenation addresses particle diversity loss that empirical exploration arms (dsMPPI [[2601.03893]] LCD determinism, UGE-MPC [[2604.12149]] Hellinger diversity, SOPPI [[2511.02015]] Stein diversity) attack without the diversity guarantee TSMC provides. Connection to trust-region MPC [[2605.07801]]: trust-region bounds the *step size* at each update; TSMC adapts the *annealing schedule* — complementary handles on "how fast to move the sampling distribution." For the project: (1) the adaptive tempering path is a principled account of why MPPI's fixed λ fails in steep/multimodal landscapes (the preconditioned-GD descent condition [[2603.24489]] is violated — TSMC annealing is the adaptive-λ answer); (2) HMC rejuvenation is the diversity-preservation mechanism the empirical exploration arms implement approximately. North-star fit: **core MPPI mechanism (particle-diversity + adaptive-annealing as the mode-collapse / convergence answer, RSS 2026) + 경로추종 (adaptive annealing = principled interpolation between prior-tracking and aggressive cost-descent) + P5 λ/covariance sweeps (TSMC adaptive-tempering as the basis for principled λ scheduling)**. Caveats: **no specific robot/diff-drive/Nav2/Gazebo platform — "trajectory and policy optimization with differentiable dynamics"; HMC rejuvenation requires dynamics gradients, which gradient-free MPPI does not have; borrow is the adaptive-tempering + particle-diversity framing as a theoretical lens, not the HMC machinery; no code surfaced; RSS 2026**. Borrow is strictly the **adaptive-tempering schedule and HMC-diversity framing as a lens for MPPI's convergence-diversity tradeoff** — when empirical exploration arms (dsMPPI [[2601.03893]] / UGE-MPC [[2604.12149]] / SOPPI [[2511.02015]]) are prototyped, use TSMC's adaptive-annealing as the account of what "adapting λ across MPPI iterations" means and ask whether a gradient-free annealing-λ schedule in nav2_mppi mimics HMC-rejuvenated diversity.
- **Suggested TODO**: <none — file as the *adaptive-tempering / HMC-particle-diversity* arm of the sampler-core theory thread (EM-MPPI [[2606.00317]] / preconditioned-GD [[2603.24489]] / trust-region [[2605.07801]]); when P5 λ/covariance sweeps are designed, use TSMC's adaptive-tempering as the account of why fixed-λ MPPI fails in steep/multimodal landscapes and ask whether a gradient-free annealing-λ schedule in nav2_mppi mimics particle-diversity; RSS 2026, differentiable-dynamics, no diff-drive/Nav2, no code.>

## 2026-07-09 08:00 — JPPD: Joint Prediction-Planning Diffusion with Differentiable Safety Guidance for Dynamic Obstacle Avoidance
- **Source**: https://arxiv.org/abs/2606.20686
- **Type**: arxiv
- **Why relevant**: Jiahao Wu, Shengwen Yu (v1 Jun 14 2026; cs.RO; ROS/Orin deployment + Isaac Sim + naturalistic pedestrian replay). **The *joint prediction-planning diffusion + differentiable safety guidance* arm for social-space navigation — notable for ROS/Orin deployment validation and for treating pedestrian prediction and robot planning as a single joint trajectory generation problem, in contrast to the predict-then-plan pipeline all P4 feed arms (DRA-MPPI [[2506.21205]], iCrowdNav [[2606.26047]], Rethinking-Gaussian [[2603.10407]]) use.** Mechanism: a **causal Transformer with cross-trajectory attention** jointly samples robot and pedestrian trajectories from a coupled distribution — the planner and predictor share one generative model rather than consuming fixed predictions; **conditional flow matching (CFM)** reduces inference steps while maintaining trajectory diversity (same CFM lineage as CFM-MPPI [[2508.01192]] and GPC [[2510.14643]], but applied to the joint prediction-planning problem); a **differentiable time-varying occupancy-probability safety guidance** replaces heuristic post-processing for safety enforcement. Evaluated on: scenario-grounded simulation + naturalistic pedestrian replay + Isaac Sim + **ROS/Orin deployment** (a physical-deployment step rare in the feed). Metrics are social-space-native: near misses, blockage time, hard-braking events — consistent with the closed-loop + human-comfort principle from [[2601.09856]] (not ADE/FDE alone). The method is not MPPI-native (end-to-end diffusion planner, contrary to the classical-planner + learned-representation thesis). Borrowable pieces: (1) the **cross-trajectory-attention joint distribution** as a P4 representation design concept — instead of consuming a marginal pedestrian prediction, condition the MPPI rollout cost on the joint robot+pedestrian distribution, encoding the prediction-planning coupling in the risk field; (2) the **differentiable time-varying occupancy-probability safety guidance** as a P3 risk-field design input folded into the MPPI collision cost. Social-space metrics (near misses / blockage / hard-braking) are concrete P5 harness axis inputs validated in ROS deployment. North-star fit: **P4 (social-space joint prediction-planning for dynamic obstacle avoidance, ROS-deployed) + P3 (differentiable occupancy-probability safety guidance as a risk-field design concept) + P5 (social-space closed-loop metrics — near miss / blockage / hard-braking as new harness axes)**. Caveats: **end-to-end diffusion planner, not MPPI-native (the joint Transformer + CFM replaces the sampler, not augments it); borrow is strictly the joint-distribution representation concept and differentiable safety potential; "intelligent transportation systems" shared-space domain — diff-drive/Nav2/Gazebo unconfirmed; no code surfaced; social-space metrics require instrumented deployment (P5 budget question)**. Borrow is the **cross-trajectory-attention joint robot+pedestrian distribution as a P4 risk channel design** (condition MPPI cost on joint prediction mass rather than a marginal prediction) and the **differentiable time-varying occupancy-probability safety guidance as a P3 risk-field design input** — prototype on a cafe crossing scene, score near-miss rate and blockage time alongside collision rate.
- **Suggested TODO**: evaluate the **cross-trajectory-attention joint robot+pedestrian distribution as a P4 dynamic risk channel design** — prototype conditioning the MPPI rollout cost on a joint prediction-planning distribution (vs consuming a marginal pedestrian prediction as DRA-MPPI [[2506.21205]] does) on a cafe crossing scene, scoring near-miss rate and blockage time (JPPD's social-space metrics) as new P5 harness axes alongside collision rate; the differentiable time-varying occupancy-probability safety guidance is the companion P3 risk-field design concept to prototype alongside.

## 2026-07-07 20:00 — DUCCT-MPPI: Dual-Uncertainty Chance-Constrained Tube MPPI for Navigation in Dynamic Environments
- **Source**: https://arxiv.org/abs/2605.28330
- **Type**: arxiv
- **Why relevant**: Benjamin Serfling, Konrad Doll, Kati Radkhah-Lens (v1 May 27 2026; cs.RO/eess.SY; physics-based simulation). **The *dual-uncertainty chance-constrained tube* arm — the first feed entry that simultaneously propagates *localization* uncertainty (Unscented Transform) and *dynamic obstacle prediction* uncertainty (Monte Carlo aggregation) into a unified MPPI chance-constraint, with a rigorous calibration evaluation methodology using proper scoring rules.** Mechanism (DUCCT-MPPI): the safety tube is expanded by two independent uncertainty sources — the UT approximates the non-Gaussian propagation of robot pose uncertainty through dynamics (localization arm), while MC aggregation builds collision-probability distributions from stochastic pedestrian predictions (prediction arm); both are integrated into a single MPPI chance-constraint cost term so each rollout is penalized by both "where is the robot, really" and "where might the pedestrian be." The proper-scoring-rules evaluation methodology is a novel contribution orthogonal to the planner: applying strictly-proper scoring rules (Brier score, logarithmic score) to the closed-loop collision-risk predictions checks whether the reported probability is *sharpness-calibrated* against realized outcomes — a stronger criterion than ECE/coverage, which cannot distinguish a calibrated-but-uninformative uniform distribution from a calibrated-and-sharp one. **Reported +28% navigation success rate over established MC-MPPI baselines.** This is structurally distinct from DRA-MPPI [[2506.21205]] (prediction-only MC arm, no localization term) and from the conformalized-rollout arms: DUCCT-MPPI is localization-uncertainty-native inside the planner, not a post-hoc calibration layer. The localization arm fills a structural gap: the feed has calibrated *prediction* uncertainty (DRA-MPPI, Rethinking-Gaussian [[2603.10407]], Scenario-aware UQ [[2512.05682]]) and calibrated *obstacle/distance-field* uncertainty (OCULAR [[2605.13028]], FCP-MPC [[2607.00776]]), but no entry that explicitly propagates *localization* uncertainty into the MPPI rollout. For the P5 σ-calibration-quality harness (Q-015): proper scoring rules are the evaluation addition — they test sharpness (not just mean coverage) of risk predictions, and the Brier/log score over a closed-loop run is a P5 harness axis complementary to reliability-diagram ECE and per-cell coverage. North-star fit: **P3 (dual-uncertainty risk field — localization + prediction in one MPPI chance-constraint) + P4 (dynamic-obstacle navigation, +28% vs MC-MPPI) + P5 (proper scoring rules as the strictly-proper calibration criterion for the σ-quality harness, beyond ECE/coverage)**. Caveats: **physics-based simulation only — no specific diff-drive/Nav2/Gazebo loop confirmed**; the UT localization arm assumes a Gaussian pose belief (not guaranteed in a multi-modal AMCL particle filter); **no code surfaced**; no head-to-head vs FCP-MPC [[2607.00776]] or the conformalized prediction arms; proper-scoring-rule evaluation requires many closed-loop episodes (a P5 budget question). Borrow is the **UT localization-uncertainty tube + proper scoring rules as the P5 calibration evaluation methodology** — add UT pose-uncertainty propagation alongside DRA-MPPI MC prediction uncertainty in nav2_mppi's chance-constraint cost (dual-uncertainty MPPI), and adopt Brier/log score over closed-loop collision-risk predictions as the strictly-proper P5 calibration metric alongside ECE.
- **Suggested TODO**: in the P5 σ-calibration-quality harness, adopt **proper scoring rules (Brier / log score)** as the evaluation criterion for collision-risk predictions alongside ECE/coverage (DUCCT-MPPI evaluation methodology), and integrate localization uncertainty (Unscented Transform pose propagation) into the nav2_mppi chance-constraint cost alongside DRA-MPPI [[2506.21205]] MC prediction uncertainty — A/B on a dynamic-obstacle cafe crossing, measuring the dual-uncertainty contribution and checking whether proper scoring catches a risk miscalibration that ECE misses.

## 2026-07-07 20:00 — Sampling-based MPC with Trust Regions via KL-Divergence Bounds
- **Source**: https://arxiv.org/abs/2605.07801
- **Type**: arxiv
- **Why relevant**: Markus Walker, Marcel Reith-Braun, Daniel Frisch, Uwe D. Hanebeck (KIT/Hanebeck group; v1 May 8 2026; eess.SY). **The *principled-update-rule* arm of the sampler-theory thread — replaces heuristic temperature/covariance tuning in sampling-based MPC (MPPI) with a trust region: a KL-divergence bound on the proposal distribution update, with an optional entropy lower bound, solved optimally via the associated Lagrangian.** Mechanism: at each MPC step the trust region constrains the proposal update within a KL ball around the old distribution — the optimal update is derived from the Lagrangian, with the Lagrange multiplier serving as a **self-tuning temperature**; an optional entropy lower bound prevents distribution collapse; combined with LCD-based deterministic sampling (the same technique as dsMPPI [[2601.03893]], same first author) for further efficiency gains. The trust region is the *step-size* arm of the sampler-theory thread: EM-MPPI [[2606.00317]] shows MPPI is an E-M step (the *family* question); preconditioned-GD [[2603.24489]] shows MPPI is a preconditioned gradient step (the *geometry* question); trust-region MPC says *the step size should be bounded by the KL divergence to the old proposal* — the principled account of why MPPI diverges when λ is too high or the cost landscape tilts too steeply. The self-tuning Lagrange-multiplier temperature is the most practical borrow: the project's P5 harness currently sweeps λ by grid search, and a trust-region-derived self-tuning λ would eliminate the most finicky nav2_mppi hyperparameter. The Hanebeck group (Walker/Reith-Braun) now contributes two feed entries: this (update-rule) + dsMPPI [[2601.03893]] (sample-construction) — together they characterize a systematic attack on MPPI's hyperparameter and noise problems from orthogonal angles. North-star fit: **core MPPI mechanism (principled proposal update / self-tuning temperature — the λ-tuning theory the P5 harness sweeps need) + the sampler-theory trio (EM-MPPI [[2606.00317]] / preconditioned-GD [[2603.24489]] / trust-region)**. Caveats: **"two benchmark environments" — no diff-drive/Nav2/Gazebo/mobile-robot confirmed**; eess.SY theory/control; **no code surfaced**; the Lagrange multiplier root-find at each step adds per-cycle cost (vs DM-MPPI [[2512.00759]] pruning budget). Borrow is the **KL-trust-region constrained proposal update with Lagrange-multiplier self-tuning temperature** as a λ-free replacement for manual temperature tuning in nav2_mppi — evaluate against grid-search λ sweeps in the P5 harness, using the EM-MPPI [[2606.00317]] exponential-family view to justify the KL-bounded M-step as the update regularizer.
- **Suggested TODO**: <none — file as the *trust-region / KL-bounded proposal update* arm of the sampler-theory thread (EM-MPPI [[2606.00317]] / preconditioned-GD [[2603.24489]]); when designing P5 λ/covariance sweeps, evaluate the Lagrange-multiplier self-tuning temperature (trust-region MPC recipe) as a λ-free alternative to grid-search; eess.SY benchmark only, no nav/diff-drive platform confirmed, no code.>

## 2026-07-07 20:00 — dsMPPI: Smooth Sampling-Based MPC via Deterministic Samples (IFAC 2026)
- **Source**: https://arxiv.org/abs/2601.03893
- **Type**: arxiv
- **Why relevant**: Markus Walker, Marcel Reith-Braun, Tai Hoang, Gerhard Neumann, Uwe D. Hanebeck (KIT/Hanebeck group; v1 Jan 7 2026, v2 Jun 2 2026; eess.SY; **IFAC 2026**). **The *deterministic-samples* arm of the smoothness/sample-efficiency thread — replaces stochastic Gaussian perturbations in MPPI with deterministic LCD samples and folds in CE-MPC iterative refinement, addressing MPPI's control non-smoothness from the *sample-construction* side without an explicit smoothness cost.** Mechanism (dsMPPI): standard MPPI draws random Gaussian perturbations → high inter-cycle variance → jerky control output; dsMPPI uses deterministic LCD sequences (covering the action space more uniformly) + MPPI's cost-weighted update + CE-MPC iterative refinement — yielding smoother rollout coverage and smoother final control sequences. **IFAC 2026** (peer-reviewed, proceedings accepted). This is the third Hanebeck-group entry (Walker/Reith-Braun) alongside trust-region MPC [[2605.07801]]: the trust region is the *update-rule* arm; dsMPPI is the *sample-construction* arm — together they characterize the group's systematic attack on MPPI's hyperparameter and noise problems from orthogonal angles. For the project the smoothness dividend maps directly to the 경로추종 north star (cross-track/smoothness/time-to-goal): vanilla MPPI's stochastic perturbations produce noisy steering commands that degrade tracking; dsMPPI attacks this from the sample-construction side while ICODE-MPPI [[2605.03260]] attacks it via continuous-time ODE residual integration — two orthogonal levers on the same symptom. Also relevant to the variance-reduction thread: variance-reducing-prior [[2602.03639]] concentrates samples near a quadratic-model region; dsMPPI reduces sampling variance by determinism — complementary mechanisms on the low-sample frontier. The exploration trade-off: deterministic samples improve coverage uniformity but may limit escape from local minima — UGE-MPC's [[2604.12149]] Hellinger-diversity step is the contrasting lever (diversity vs smoothness). North-star fit: **core MPPI mechanism (deterministic sample construction as a smoothness + sample-efficiency lever, IFAC 2026) + 경로추종 smoothness (less noisy control output) + the Hanebeck sampler-theory pair [[2605.07801]]**. Caveats: **no specific robot/nav platform confirmed — eess.SY framing**; deterministic samples reduce exploration variance but may limit local-minima escape; CE refinement adds per-cycle cost vs the DM-MPPI [[2512.00759]] pruning budget; **no code surfaced**. Borrow is the **deterministic LCD samples + CE-MPC iterative refinement as a drop-in sample-construction smoothness upgrade for nav2_mppi** — prototype and measure steering chatter / cross-track error vs vanilla Gaussian baseline, contrasted with ICODE-MPPI [[2605.03260]] ODE smoothness and UGE-MPC [[2604.12149]] diversity, checking the exploration trade-off on a cafe corner scene.
- **Suggested TODO**: <none — file as the *deterministic-samples / smoothness* arm of the sample-construction thread (Hanebeck group: dsMPPI + trust-region [[2605.07801]]); when the P5 경로추종 (cross-track/smoothness) sweep is run, prototype LCD deterministic samples + CE refinement as a drop-in change for nav2_mppi and measure smoothness vs vanilla stochastic Gaussian, contrasted with ICODE-MPPI [[2605.03260]] ODE smoothness and UGE-MPC [[2604.12149]] Hellinger diversity; IFAC 2026, eess.SY, no nav/diff-drive platform confirmed.>

## 2026-07-06 20:00 — Variance-Reduced MPPI via Quadratic Model Approximation
- **Source**: https://arxiv.org/abs/2602.03639
- **Type**: arxiv
- **Why relevant**: Fabian Schramm, Franki Nguimatsia Tiofack, Nicolas Perrin-Gilbert, Marc Toussaint, Justin Carpentier (RSS 2026, Sydney; v1 Feb 3 2026, v2 May 29 2026; cs.RO/cs.LG). **The *variance-reducing-prior* arm of the sample-efficiency thread — pre-training-free, requiring no offline generative model: it decomposes the MPPI objective into a tractable approximate-model component + residual, fits a quadratic approximation to the approximate model, and uses it as a closed-form sample-concentrating prior that reduces the variance of the MPPI weighted-average update in the low-sample regime.** Mechanism: the cost is split into a quadratic-approximable part (yielding a closed-form Gaussian prior over trajectories) and a residual (sampled by MPPI as usual); the sampling distribution concentrates in high-quality trajectory regions before any rollout, with variance reduction proportional to the quadratic fit quality — validated on nonlinear underactuated cart-pole + contact-rich manipulation at RSS 2026. The sample-efficiency thread covers: proposal-pre-training (GPC [[2510.14643]]), guided generation (CFM-MPPI [[2508.01192]]), influence-pruning (DM-MPPI [[2512.00759]]), scenario-conditioned prior (HOLO-MPPI [[2606.16480]]) — this adds a **quadratic-model prior as a pre-training-free variance reducer**, most attractive when P2/P3 heavy front-ends (learned dynamics + BEV) tighten the per-cycle compute budget (fewer rollouts ≡ higher variance — variance reduction and sample-count reduction are the same lever). For P2: if the dynamics residual has a quadratic-approximable component (linearized prior around the nominal trajectory), the prior concentrates samples near the feasible region; for P3: if the risk-field gradient is locally quadratic near obstacles, the prior sharpens collision-avoidance samples without a pre-trained cost proxy. Structurally distinct from all proposal-learning arms (GPC, CFM-MPPI, HOLO-MPPI): they amortize an offline pre-training cost; this closes the loop entirely within the MPPI iteration using local second-order structure. North-star fit: **sample-efficiency axis (pre-training-free variance reduction as a complement to proposal learning) + P2/P3 (applicable when cost or dynamics includes a tractable quadratic-approximable component)**. Caveats: **platform is cart-pole + contact-rich manipulation (RSS 2026) — no diff-drive / Nav2 / Gazebo; no code release mentioned; variance-reduction guarantee degrades for highly nonlinear / multimodal costs (the dynamic risk field near obstacles may not be well-quadratically approximable); no head-to-head vs DM-MPPI [[2512.00759]] or GPC [[2510.14643]] on a mobile nav task; the derivative-based path requires a differentiable approximate model, which the BEV/risk-field stack may not always provide**. Borrow is strictly the **quadratic-approximate-model prior as a pre-training-free MPPI variance reducer** — when the P2/P3 sample budget is tight, test whether a local quadratic approximation of the dynamics-prior component concentrates samples better than DM-MPPI [[2512.00759]] influence-pruning on cafe-straight at a fixed rollout count.
- **Suggested TODO**: <none — file as the *variance-reducing-quadratic-prior* arm of the sample-efficiency thread; when MPPI rollout budget is constrained by P2/P3 heavy front-ends, compare quadratic-model-prior concentration (pre-training-free) vs DM-MPPI [[2512.00759]] influence-pruning and GPC [[2510.14643]] flow-matching warm-start on cafe-straight at fixed rollout count; concept-only borrow, RSS 2026, cart-pole + manipulation platform, no diff-drive/Nav2/code.>

## 2026-07-06 00:00 — iCrowdNav: Learning Robot Visual Navigation in Crowds via Intention-Aware Scene Representations
- **Source**: https://arxiv.org/abs/2606.26047
- **Type**: arxiv
- **Why relevant**: Han Bao, Bingyi Xia, Hanjing Ye, Yu Zhan, Hao Cheng, Baozhi Jia, Wenjun Xu, Jiankun Wang (same group as NavIsaacLab [[2606.26265]]; v1 Jun 24 2026; cs.RO). **The *intention-aware visual representation* arm for crowd navigation — a structural complement to NavIsaacLab [[2606.26265]] (which generates realistic crowds) that attacks the representation question: how should the planner *encode* pedestrians?** Mechanism (iCrowdNav): a **spatio-temporal occupancy encoder** captures scene geometry from egocentric visual input, and an **Intent-Interact Former (I²Former)** — a pose-driven cross-attention module — predicts pedestrian motion intentions and injects them into a compact state representation that drives a deep RL policy. The representation insight: treating pedestrians as *intention-bearing agents* (where they are *going*, not just where they are) is a richer encoding than a static-obstacle costmap entry, and the I²Former is a modular component separable from the RL policy — pose + history → predicted intention. For the P4 dynamic risk channel, I²Former is the borrowable piece: replace point-obstacle encoding with an intention-aware pedestrian representation that predicts each agent's trajectory distribution and weights the dynamic risk field accordingly, the same role the pedestrian-forecasting arms (Rethinking-Gaussian [[2603.10407]], Scenario-aware UQ [[2512.05682]], DRA-MPPI [[2506.21205]]) fill from the *prediction-consumer* side. NavIsaacLab [[2606.26265]] (same group) provides the physics-based crowd simulation needed to train such a representation, closing a dataset→encoder→risk-field loop within one lineage. North-star fit: **P4 (dynamic obstacle representation — intention-aware pedestrian encoding for the dynamic risk channel) + P3 (the I²Former attention mechanism as a semantic pedestrian-intent sub-layer in the BEV risk field)**. Caveats: **deep RL policy (end-to-end), not MPPI/sampling-based MPC — the borrow is strictly the I²Former intention-prediction component, not the RL policy**; robot platform unspecified; no code surfaced; the RL cooperative-agent assumption does not transfer to non-cooperative pedestrians (flagged by [[2601.09856]]). Borrow is strictly the **I²Former pose-driven cross-attention as a modular pedestrian-intent predictor** for the P4 dynamic risk channel — decode each pedestrian's predicted trajectory distribution from recent pose history, weight the dynamic risk field by intention-confidence, and contrast with point-obstacle encoding on a cafe crossing scene.
- **Suggested TODO**: evaluate the **I²Former pose-driven cross-attention mechanism as a pedestrian intention predictor** for the P4 dynamic risk channel — replace point-obstacle encoding with an intention-aware pedestrian representation (predicted trajectory distribution from recent pose history), weight the dynamic risk field by intention-confidence, A/B vs Rethinking-Gaussian [[2603.10407]] recalibrated-covariance on a cafe crossing scene; use NavIsaacLab [[2606.26265]] (same group) as the crowd-simulation candidate for training data.

## 2026-07-05 16:00 — Continual Robot Policy Learning via Variational Neural Dynamics
- **Source**: https://arxiv.org/abs/2606.27353
- **Type**: arxiv
- **Why relevant**: Jiaxu Xing, Zhiyuan Zhu, Yunfan Ren, Ismail Geles, Yifan Zhai, Rudolf Reiter, Davide Scaramuzza (Scaramuzza group; v1 Jun 25 2026; cs.RO; quadrotor hardware). **The *condition-aware continual dynamics* arm of the P2 learned-dynamics bottleneck — closes the multi-venue adaptation gap the feed’s static-offline residual arms leave open: it continuously adapts online as the robot encounters changing conditions (wind, varying payload, hardware wear), using a recurrent encoder that identifies recurring dynamics patterns and adapts 5× faster than re-fitting from scratch.** Mechanism: combines an **analytical physics prior with a neural residual**, then runs a **recurrent encoder that infers a hidden “condition” variable from recent state-action interaction history**; both the residual prediction and the policy are conditioned on this inferred latent condition, so when the encoder recognizes a recurring disturbance pattern it immediately applies the learned correction — reported **65.7% hover error reduction and 53.3% tracking error reduction** vs baseline, with **5× faster recovery** from recurring disturbances. Structurally distinct from the feed’s other P2 residual arms: ICODE-MPPI [[2605.03260]] integrates the residual as a continuous-time ODE but trains offline and has no condition inference; PMC12788286 uses a discrete ensemble-mean without regime awareness; GP-residual [[2602.16206]] and energy-regularized NN [[2604.14678]] are also offline — only this paper adds an **online condition-identification loop** that recognizes which dynamical regime the robot is in and adapts without re-training. For the project the inferred condition variable is the multi-venue adaptation mechanism: the recurrent encoder would distinguish café (narrow, tight turning, floor friction) from small_city (open, fast, different surface) from real-world rosbag (unknown disturbance) and condition the residual accordingly — without per-venue re-training. North-star fit: **P2 (the STATE bottleneck: learned-dynamics ensemble + online adaptation via condition inference) + the three-venue generalization requirement (café / small_city / 미관측 rosbag, each a distinct dynamical condition)**. Caveats: **platform is a quadrotor — no diff-drive, no Nav2, no Gazebo, no crowd; the recurrent encoder is over state-action history, not perception/BEV; no code surfaced; the 5× recovery claim is for *recurring* disturbances in similar conditions — first contact with a truly novel venue still requires initial condition learning before recognition kicks in**. Borrow is strictly the **physics-prior + neural-residual + recurrent-condition-encoder architecture** as the P2 residual dynamics interface — prototype a condition-aware residual that infers current venue/disturbance from recent rollout history and conditions the residual output, A/B vs a static offline residual on a café→small_city venue switch.
- **Suggested TODO**: prototype a **condition-aware residual dynamics model** (Variational Neural Dynamics recipe): physics prior + neural residual + recurrent encoder that infers hidden condition (venue/disturbance regime) from recent state-action history — condition both the residual output and any P3 σ channel on the inferred latent, A/B vs a static offline residual on a café→small_city venue switch for recovery speed and cross-track error; the condition encoder is the multi-venue adaptation mechanism the static P2 residual arms lack.

## 2026-07-05 16:00 — Model Predictive Path Integral PID Control for Learning-Based Path Following
- **Source**: https://arxiv.org/abs/2603.29499
- **Type**: arxiv
- **Why relevant**: Teruki Kato, Koshi Oishi, Seigo Ito (v1 Mar 31 2026; eess.SY; mini forklift with real driving data; submitted to IFAC Journal of Systems and Control). **The *gain-space sampling + residual dynamics* arm of the 경로추종 thread — directly attacks both path-following quality and P2 residual-dynamics integration by reducing MPPI’s optimization dimensionality from the full T×n-D control-sequence space to a 3D PID-gain space.** Mechanism: instead of sampling full control sequences (high-dimensional), MPPI optimizes **PID gain parameters (K_p, K_i, K_d) in a 3D space** and the chosen gains drive a physical PID controller; the plant dynamics are modeled by a **residual-learning model that integrates a physical (analytical) prior with a neural network** for system identification on real driving data — yielding improved sample efficiency, reduced control chattering, and comparable path-tracking performance to conventional MPPI. This is the *gain-space* pole of the MPPI-for-path-following axis the project’s 경로추종 north star (cross-track error / heading error / smoothness / time-to-goal) sits on: where ICODE-MPPI [[2605.03260]] buys smoothness via continuous-time ODE residual integration, MPPI-PID argues the **structure of the PID controller is itself a prior that reduces effective optimization dimension and buys input continuity without an explicit smoothness cost**. The residual dynamics (physics + NN) is also a direct P2 interface — it learns the gap between the analytical model and real-world vehicle behavior from data, the same learning target the P2 ensemble bottleneck needs. North-star fit: **경로추종 completeness (cross-track, heading, smoothness, time-to-goal directly addressed) + P2 (residual-learning dynamics from real data + a structured low-dimensional optimization interface MPPI can exploit)**. Caveats: **platform is a mini forklift (non-holonomic, Ackermann-ish) — no diff-drive / Nav2 / Gazebo; no dynamic/pedestrian obstacle context; IFAC journal submission, arxiv preprint at 2603.29499; PID-gain-space is 3D when path-following error is 1D (lateral) — more general nav may need compositional gain banks; no code surfaced; no head-to-head vs ICODE-MPPI [[2605.03260]] or full-sequence MPPI on the same smoothness / cross-track bench**. Borrow is strictly the **PID-gain-space MPPI + physics-NN residual dynamics as a 경로추종 efficiency lever** — prototype as a low-dimensional path-following mode for nav2_mppi on café-straight, A/B vs full-sequence MPPI and ICODE-MPPI [[2605.03260]] on cross-track / smoothness / sample-count tradeoff.
- **Suggested TODO**: prototype **MPPI-PID path-following** (sample PID gain space instead of full control sequences) on nav2_mppi’s café-straight track: use a physics + NN residual dynamics model (real diff-drive data) to evaluate the low-dimensional gain-space sampling vs full-sequence MPPI on cross-track error / input smoothness / sample-budget, and A/B vs the ICODE-MPPI [[2605.03260]] continuous-time-ODE-residual baseline — the gain-space dimension reduction is the lever to compare against DM-MPPI [[2512.00759]] pruning and UGE-MPC [[2604.12149]] diversity on a fixed sample budget.

## 2026-07-05 12:00 — HOLO-MPPI: Multi-Scenario Motion Planning via Hierarchical Policy Optimization
- **Source**: https://arxiv.org/abs/2606.16480
- **Type**: arxiv
- **Why relevant**: Youngjae Min, Jovin D'sa, Faizan M. Tariq, David Isele, Navid Azizan, Sangjae Bae (Jun 15 2026; cs.RO). **The *scenario-conditioned learned prior* arm for MPPI sampling — the pattern that directly instantiates the project's core hypothesis (representation quality → planning quality) at the sampler boundary.** Mechanism: a **two-tier hierarchical approach** where an **offline high-level policy** learns to generate **scenario-robust plans** that **parameterize MPPI's sampling distribution** conditioned on the current observation and goal, enabling real-time MPPI refinement without per-scenario retuning. This is structurally distinct from all proposal-learning arms already in the feed: GPC [[2510.14643]] trains a conditional flow-matching proposal on the sampler's *own rollouts* (self-bootstrapping — the proposal is learned from the sampler's history); CFM-MPPI [[2508.01192]] bridges a guided flow-matching bank with MPPI in a generation-refinement loop (iterative refinement); DM-MPPI [[2512.00759]] prunes by learned influence (cost-side pruning, not prior shaping); HOLO-MPPI asks a different question: *what kind of trajectories should I sample given I am in THIS scene right now?* — the **observation-conditioned sampling prior** rather than a rollout-history-conditioned or cost-pruned sampler. The **multi-scenario deployment** challenge it addresses (avoid per-scenario retuning; the learned prior must generalize across qualitatively different scenes) is exactly the north star's three-venue requirement: café (narrow indoor) + small_city (open outdoor) + real-world rosbag (미관측 distribution) — a fixed Gaussian prior with hand-set covariance is exactly the kind of retuning-required artifact HOLO-MPPI replaces. The P1/P2 interface reads naturally: the BEV/semantic representation front-end is the observation input to the high-level policy; the policy outputs the MPPI sampling distribution parameters (mean, covariance, or a latent from which samples are drawn); the low-level MPPI refines online — so the learned prior becomes an *output of the representation front-end* that directly conditions the sampler, precisely the "representation quality determines planning quality" loop. North-star fit: **core MPPI mechanism (scenario-conditioned sampling prior as the observation-to-sampler bridge) + representation-aware-proposal thesis (the learned representation parameterizes the sampler, not just cost functions) + multi-venue generalization (replaces per-venue manual prior tuning)**. Caveats: **autonomous vehicle / driving scenarios — no diff-drive, no Nav2, no Gazebo, no crowd/pedestrian context; "multi-scenario" refers to driving scenario types (intersections, merges), not indoor/outdoor nav scene diversity**; **no code surfaced** from the abstract; whether a BEV representation encodes enough scene-discriminating signal to differentiate a café corridor from a small_city plaza and condition the prior accordingly is the open implementation question; the offline policy training loop requires a scenario-diverse training set (the project's sim environments are the candidate). Borrow is strictly the **offline-learned policy that conditions MPPI's sampling distribution parameters on the current observation + goal as a scenario-adaptive prior** — prototype it as the interface between the P1 BEV representation front-end and nav2_mppi's sampler, replacing the fixed Gaussian prior, and A/B on café vs small_city without per-scene retuning vs the GPC [[2510.14643]] self-bootstrapping and CFM-MPPI [[2508.01192]] refine-loop proposals.
- **Suggested TODO**: prototype a **BEV-conditioned MPPI sampling prior** (HOLO-MPPI pattern): offline-train a policy that takes P1 BEV features as input and outputs nav2_mppi's sampling distribution parameters, replacing the fixed Gaussian prior — A/B on café vs small_city without per-scene retuning vs the GPC [[2510.14643]] self-bootstrapping and fixed-prior baselines; the learned prior is the representation-to-sampler bridge.

## 2026-07-05 00:00 — Generalized MPPI Control as Expectation–Maximization (EM-MPPI)
- **Source**: https://arxiv.org/abs/2606.00317
- **Type**: arxiv
- **Why relevant**: Jiarui Wang, Sina Sharifi, Mahyar Fazlyab (v1 May 29 2026; eess.SY). **The *sampler-core foundations* arm the mode-keeping / non-Gaussian-proposal thread has been circling empirically but never grounded theoretically — it shows vanilla MPPI is a *special case of Expectation–Maximization* on a probabilistic-inference view of optimal control, then generalizes the proposal from the fixed Gaussian to any *exponential-family* distribution with local-convergence rates.** Mechanism: cast trajectory optimization as inference, and the MPPI weighted-average update becomes the M-step of EM against a Gibbs-tilted posterior; the fixed-covariance Gaussian is just one exponential-family choice, so **EM-MPPI opens the proposal to exponential-family parameterizations** and characterizes local convergence in terms of the posterior-covariance vs exploration-covariance ratio (sufficient-increase of the log-likelihood under strongly-convex log-partition). This is the *principled* backbone under the feed's empirically-motivated proposal-shape arms: SVG / SOPPI [[2511.02015]] argue for Stein/multi-modal proposals, OT-MPC [[2605.02147]] for an entropy-regularized-OT update, ELVIS [[2605.04709]] for Gaussian-mixture MPPI to avoid mode-averaging — EM-MPPI says *why* those are legitimate (they are M-steps over richer families) and hands a convergence lens for choosing the family. It has a **companion paper from the same group, [MPPI as Preconditioned Gradient Descent](https://arxiv.org/abs/2603.24489)** (Fazlyab, Sharifi, Wang; v2 May 22 2026; math.OC) — the KL/free-energy dual view where the classical MPPI update is *exactly* a unit-step preconditioned-gradient step and the preconditioner is the Gibbs-tilted covariance, which is the theoretical account of *why covariance adaptation (SVG-MPPI) changes the step geometry*. North-star fit: **core MPPI mechanism (the proposal-distribution axis the whole sampler thread turns on) + the 경로추종 quality that better-conditioned / mode-keeping proposals buy + a design lens for P2/P3 when a learned residual or risk field makes the tilted posterior non-Gaussian**. Caveats: **eess.SY/math.OC theory — no robot / diff-drive / Nav2 / Gazebo experiments surfaced, no quantitative nav numbers, code "upon acceptance"; the exponential-family generalization is stated abstractly (no concrete non-Gaussian proposal implemented in the abstract); it explains and unifies rather than beats the empirical arms.** Borrow is strictly the **EM / preconditioned-GD framework as the theoretical selector for *which* non-Gaussian proposal family to adopt** — when the P3 risk-aware cost or a learned residual makes the Gibbs-tilted posterior genuinely non-Gaussian, use the exponential-family-EM view to pick the proposal (vs the empirically-motivated SOPPI [[2511.02015]] / OT-MPC [[2605.02147]] / GM-MPPI [[2605.04709]] arms) and the covariance-preconditioner view to reason about SVG-MPPI-style covariance adaptation as step-geometry conditioning.
- **Suggested TODO**: <none — file as the *theoretical foundations* arm of the sampler-core / non-Gaussian-proposal thread; when a non-Gaussian proposal is prototyped (SOPPI [[2511.02015]] / OT-MPC [[2605.02147]] / GM-MPPI [[2605.04709]]), use the EM exponential-family view to justify the family choice and the companion [preconditioned-GD](https://arxiv.org/abs/2603.24489) covariance-as-preconditioner view to reason about SVG-MPPI covariance adaptation; concept/theory-only borrow — no nav experiments, no code, no diff-drive.>

## 2026-07-05 00:00 — Generative Predictive Control: Conditional Flow-Matching Proposals From and For Sampling-Based MPC
- **Source**: https://arxiv.org/abs/2510.14643
- **Type**: arxiv
- **Why relevant**: Lara Brudermüller, Brandon Hung, Xinghao Zhu, Jiuguang Wang, Nick Hawes, Preston Culbertson, Simon Le Cleac'h (v2 Jan 7 2026; cs.RO). **The *amortize-the-proposal* arm the generative-proposal thread was missing — where CFM-MPPI [[2508.01192]] / Crowd-FM generate a bank and then refine/select, this one trains a conditional flow-matching model *on the sampler's own SPC rollouts* so the generative model becomes the online proposal distribution, folding iterative-refinement cost into a single informed sample step ("from and for" sampling MPC).** Mechanism (Generative Predictive Control): collect sampling-based-predictive-control (SPC) trajectories in simulation, train a **conditional flow-matching model on that noisy control-sequence data**, then at deploy time the generative model **proposes informed control sequences upfront** that bootstrap/warm-start the online sampler — amortizing the iterative refinement a cold Gaussian proposal needs, reported to improve sample efficiency and shorten the required planning horizon with robust generalization across task variants. This is the *self-bootstrapping* pole of the generative-proposal thread: CFM-MPPI [[2508.01192]] refines a CFM bank with MPPI and warm-starts the next generation; Crowd-FM adds a learned human-likeness *selector*; DM-MPPI [[2512.00759]] prunes samples by learned influence; GPC instead **learns the proposal from the sampler's own history and hands it back to the sampler** — directly the "learned input representation *is* the proposal" bet in the project's core hypothesis, applied to the action distribution rather than the perception field. North-star fit: **core MPPI mechanism (a learned proposal that shrinks per-cycle sample cost — the compute-budget lever the heavy-front-end phases need, complementary to DM-MPPI [[2512.00759]] pruning and Feedback-MPPI re-plan-rate) + the representation-aware-proposal thesis (amortize the sampler with a generative model trained on its own rollouts)**. Caveats: **platform is a *quadruped* doing contact-rich loco-manipulation — out of the project's diff-drive / nav scope, so the borrow is strictly the *train-a-flow-matching-proposal-on-your-own-SPC-rollouts-and-warm-start-with-it* recipe, not the task**; no Nav2 / Gazebo / diff-drive closed loop; **code-release status not surfaced in the abstract**; the proposal is trained offline in sim and its transfer when the online cost changes (a P3 risk-aware cost) is the same open risk DM-MPPI [[2512.00759]] carries; no head-to-head vs the CFM-MPPI [[2508.01192]] refine-loop. Borrow is strictly the **conditional-flow-matching proposal trained on the sampler's own rollouts as an amortized warm-start for nav2_mppi** — prototype it as the proposal-generation stage that shrinks the sample budget, contrasted with the refine-loop (CFM-MPPI [[2508.01192]]), select-head (Crowd-FM), and influence-pruning (DM-MPPI [[2512.00759]]) poles, with the caveat that it is a quadruped-manipulation result and the offline-trained proposal must be re-checked when a P3 risk cost changes the objective online.
- **Suggested TODO**: <see Notion — prototype a **conditional-flow-matching proposal trained on nav2_mppi's own SPC rollouts** (GPC "from and for" recipe) as an amortized warm-start that shrinks the per-cycle sample budget on cafe-straight, A/B vs a cold Gaussian proposal and vs the DM-MPPI [[2512.00759]] influence-pruning lever; check whether the offline-trained proposal survives a P3 risk-aware cost change online — concept borrow only, source platform is quadruped loco-manipulation, not diff-drive.>

## 2026-07-04 16:00 — NavIsaacLab: Generating Realistic Crowd via Parallel Robot Learning for Benchmarking Human-aware Navigation
- **Source**: https://arxiv.org/abs/2606.26265
- **Type**: benchmark
- **Why relevant**: Bingyi Xia, Han Bao, Jingyu Zhu, Hanjing Ye, Yuhan Pang, Guangcheng Chen, Liang Lin, Wenjun Xu, Jiankun Wang (v1 Jun 24 2026; cs.RO; built on Isaac Lab). **The feed's first *benchmarking / crowd-simulation* entry — nearly every prior arm is a planner or a calibration recipe; this is the evaluation *substrate* they would be scored on.** NavIsaacLab generates realistic pedestrian crowds via a **trajectory-diffusion model + adversarial-motion-learning controller** — controllable, *physics-based* pedestrian simulation rather than handcrafted social-force / scripted-actor rules — on **GPU-parallel photo-realistic Isaac Lab scenes** across "diverse cross-scale scenes," for both benchmarking and training human-aware navigation policies, motivated by the scarcity of diverse high-quality crowd scene data. Relevance is two-fold. (1) **P5 evaluation harness**: the north star's "완벽" is nailed down by a quantitative metric set over crowd scenarios, and a physics-based, diffusion-driven crowd generator is a far stronger stress-test of dynamic 회피 than scripted Gazebo actors / PedSim — the *diffusion + adversarial* crowd-generation recipe is portable as a scenario generator even if the sim engine is not adopted. (2) **P4 dynamic-obstacle track**: controllable realistic multi-pedestrian scenes are exactly the D-stage substrate the dynamic-risk channel needs to be exercised against. North-star fit: **P4 (dynamic 동적 장애물 scenario generation) + P5 (crowd evaluation substrate / external benchmark to compare our nav2_mppi stack against) + the 미관측-distribution clause (diverse cross-scale scenes stress distribution shift)**. Caveats: **Isaac Lab, not the project's Gazebo Harmonic / Nav2 / Cyclone DDS stack** — adopting the engine is a sim-stack decision, so the borrow is the *crowd-generation methodology (trajectory-diffusion + adversarial motion) and the cross-scale benchmark taxonomy*, not the harness itself; **no MPPI / sampling-planner tie-in** (it benchmarks navigation policies generically, planner-agnostic — it does not evaluate a sampler-native cost); it **trains policies via parallel RL** (an end-to-end flavor orthogonal to the classical-planner-+-learned-representation thesis — the value here is the *crowd sim + benchmark*, not the RL policy); **diff-drive focus unconfirmed** ("robot autonomous navigation" stated generically); **code-release status not surfaced in the abstract** (check on read). Borrow is strictly the **diffusion + adversarial physics-based crowd-generation as a P4/P5 pedestrian scenario generator** (vs scripted actors / PedSim) and the **cross-scale benchmark taxonomy as an external comparison target** for the project's Gazebo cafe/small_city dynamic scenes.
- **Suggested TODO**: <see Notion — evaluate porting the NavIsaacLab **trajectory-diffusion + adversarial-motion crowd-generation** recipe as a physics-based pedestrian scenario generator (vs scripted Gazebo actors / PedSim) for the cafe/small_city dynamic scenes, and use its cross-scale benchmark taxonomy as an external comparison target when the P5 metric set + P4 dynamic-obstacle scenarios are specced; sim-stack is Isaac Lab not Gazebo/Nav2, so borrow the crowd-gen methodology, not the engine — check code release on read.>

## 2026-07-04 12:00 — EXACT-MPPI: Exact Signed-Distance Navigation for Arbitrary-Footprint Robots from Point Clouds via Path Integral Control
- **Source**: https://arxiv.org/abs/2605.29663
- **Type**: arxiv
- **Why relevant**: Chen Peng, Zhikang Ge, Wenwu Lu, Haiming Gao, Stavros Vougioukas, Peng Wei (UC Davis ag-robotics lineage; v1 May 28 2026, **v2 Jun 2 2026**; cs.RO). **The *representation-beyond-costmap* arm stated almost verbatim in the project's core hypothesis — it maps local point-cloud observations + sparse guidance *directly to motion commands with no intermediate map representation*, folding an analytic *exact* signed-distance field for arbitrary non-convex polygon footprints straight into the MPPI collision cost.** This is the rare feed entry that is MPPI-native *and* diff-drive-native *and* a deliberate perception-representation move: "maps local point-cloud observations and sparse guidance directly to motion commands, without any intermediate map representation," with an **analytic signed-distance calculator integrated into MPPI**, footprints as **simple (convex *or concave*) polygons** plus a **rectangle-cover optimization for rectilinear shapes**, everything **batched in JAX for GPU real-time receding-horizon control**, and — crucially for portability — it "deploys on differential-drive, Ackermann, omnidirectional, and hybrid-mode platforms without per-platform training." Two things make it worth logging on the σ/representation branch. (1) **Representation locus**: where nearly every recent feed arm calibrates or learns a *field* (dynamics residual σ, latent, pedestrian forecast, traversability), EXACT-MPPI *replaces the costmap itself* with a mapless point-cloud→exact-SDF representation the sampler queries directly — precisely the "evolve the MPPI input representation past the costmap" thesis in CLAUDE.md, but from the *geometry-exact*, not the learned-semantic, direction. (2) **Distance-field duality with the topmost entry**: FCP-MPPI [[2607.00776]] *conformalizes* the distance field into a calibrated distribution-free lower bound (the uncertain/probabilistic pole of "what distance does the sampler query"); EXACT-MPPI computes that same distance field **analytically and exactly from the true footprint geometry** (the deterministic/exact pole) — the two are complementary loci on the collision-geometry axis, and the honest question is whether *exact non-convex footprint geometry* buys tight-space feasibility that a conformalized-but-circle/convex-approximated footprint cannot. Most MPPI collision costs use a circumscribed-radius or convex hull; EXACT reasons about the true non-convex shape, which is exactly the north-star's **가까운·좁은 (close/narrow)** cafe-passage clause where a convex over-approximation freezes the robot. North-star fit: **object-avoidance completeness (static, tight-space, arbitrary-footprint) + the core-hypothesis representation-beyond-costmap loop + diff-drive-native MPPI + P1 (a mapless geometry front-end alternative to the BEV/costmap stack)**. Caveats: **static-geometry only — no dynamic/pedestrian obstacle, no uncertainty/aleatoric/epistemic channel at all** (it is exact deterministic geometry, so it is *orthogonal* to the entire P3 risk/σ-calibration thread and the STATE P2-ensemble bottleneck — it hardens *where the obstacle is*, not *how uncertain* the model is); the representation is a **raw point-cloud footprint SDF, not a learned semantic BEV** (no semseg / traversability class — it answers "is this cell occupied geometry" not "what is it"); **platform/simulator not pinned — claims diff-drive/Ackermann/omni deploy but no Nav2 / Gazebo closed loop confirmed from the abstract**; the stack is **JAX-batched, not the nav2_mppi C++ critic path** (the borrow is the *exact-non-convex-footprint-SDF-from-point-cloud-as-MPPI-collision-cost concept + the mapless no-intermediate-representation framing*, not the JAX machinery); **no code-release status surfaced; no headline success/collision numbers in the abstract** (claims are feasibility-preservation where convex-footprint planners fail + faster batched distance eval, not a benchmarked success rate). Borrow is strictly the **exact non-convex footprint signed-distance-field computed directly from the local point cloud and folded into the MPPI collision cost, with no intermediate costmap** — evaluate whether true-footprint-exact geometry unlocks a cafe-narrow passage that the current circumscribed-radius / convex collision cost freezes on, and file it as the *geometry-exact, deterministic* counterpole to the conformalized-distance-field FCP-MPPI [[2607.00776]] on the collision-geometry axis (Q-015 asks *where* we calibrate; this asks whether the sampler's queried distance should be *calibrated* or *computed exactly* in the first place).
- **Suggested TODO**: prototype an **exact non-convex footprint SDF collision cost for nav2_mppi from the local point cloud** (EXACT-MPPI recipe): replace the circumscribed-radius / convex-hull collision term with an analytic signed-distance to the true robot polygon, batched over rollouts, and A/B on a cafe-narrow / tight-passage scene — does true-footprint geometry preserve a feasible squeeze-through the convex over-approximation freezes on, and what is the per-rollout distance-eval cost against the DM-MPPI [[2512.00759]] pruning budget; file as the *geometry-exact, deterministic* counterpole to the conformalized-distance-field FCP-MPPI [[2607.00776]] (does the sampler's queried distance want to be calibrated-uncertain or computed-exact — and can they compose: exact geometry + conformal residual margin).

## 2026-07-04 08:00 — FCP-MPC: From Prediction Uncertainty to Conformalized Distance Fields for Safe Motion Planning
- **Source**: https://arxiv.org/abs/2607.00776
- **Type**: arxiv
- **Why relevant**: Jaeuk Shin, Yoonseok Ra, Insoon Yang (SNU; v1 Jul 1 2026; cs.RO/eess.SY; ETH–UCY pedestrian benchmarks + 3D quadrotor with up to 280 obstacles). **The *sampler-native, field-level* conformal-calibration arm the feed's guarantee thread hadn't landed — it conformalizes the *entire distance field* (not a scalar score, a per-cell value, a latent, or a rollout) into a distribution-free field-level lower bound and folds that envelope into a *sampling-based* MPC as a tightened safety constraint.** Mechanism (FCP-MPC): residual distance fields exhibit **low-rank structure + temporal stability**, so an envelope is **fitted offline via functional PCA + a Gaussian-mixture inductive conformal procedure**, then **refined online by a lightweight adaptive functional conformal (AFCP) update**; the conformalized envelope becomes a tightened collision constraint the sampler respects, reaching goals where **pointwise and egocentric conformal baselines are too conservative or too expensive**, at per-step compute well below uncertainty-reasoning alternatives. This is structurally distinct from every conformal arm already in the feed: OCULAR [[OCULAR-conformal-dynamics-uncertainty]] conformalizes the *dynamics model's* uncertainty per-cell from images; DRA-MPPI [[2506.21205]] conformalizes *rollout selection*; CSRC [[2606.04185]] gives a *global* coverage set; Pixels-to-Proofs [[2606.15594]] conformalizes the *learned latent's* error; Scenario-aware UQ [[2512.05682]] and Rethinking-Gaussian [[2603.10407]] conformalize/recalibrate the *other-agent forecast* — FCP-MPC is the only one that calibrates the **distance/obstacle-geometry field the planner queries directly**, and (unlike the SLS-robust Pixels-to-Proofs and the CBF-QP filters) it does so **inside a sampling-based MPC**, which is what makes the borrow portable toward nav2_mppi. For the σ-calibration-quality axis (Q-015) it answers the "*where* do we calibrate" question with a new locus — the collision-geometry field — and its **AFCP online update** is an adaptation mechanism to compare against the Bayesian-consistency shrink [[2511.21871]] ("does σ tighten correctly as data accrues"). North-star fit: **P3 (a coverage-guaranteed obstacle/distance field — a calibration locus distinct from per-cell/latent/rollout/forecast) + P4 (ETH–UCY pedestrian navigation, dynamic 회피) + P5 (field-level functional-conformal coverage + the AFCP adaptation rate are guarantee-bearing calibration-quality knobs the σ-calibration harness can sweep, beyond scalar ECE)**. Caveats: it calibrates **obstacle/collision geometry, not the P2 learned-dynamics residual σ nor an epistemic-ensemble spread** — so it is *orthogonal* to the STATE bottleneck's dynamics-σ (it hardens the distance field the sampler queries, not the ensemble posterior); **ETH–UCY replay + aerial quadrotor — no diff-drive / Nav2 / Gazebo confirmed**, and the quadrotor demo is 3D free-space, not indoor ground nav; the **functional-PCA + GMM-inductive-conformal offline fit + AFCP online loop is a nontrivial pipeline** (low-rank/temporal-stability assumptions on the residual field must hold for a cluttered cafe); it is "sampling-based MPC" but **MPPI-specificity is unconfirmed** from the abstract; **no code surfaced**. Borrow is strictly the **conformalize-the-whole-distance-field-into-a-tightened-sampler-constraint recipe + the AFCP online-adaptation loop** as a P3 obstacle-field calibration locus and a P5 field-level-coverage metric — evaluate whether calibrating the *distance field the sampler queries* catches a collision-geometry miscalibration that the dynamics-residual σ (OCULAR-style, or the P2 ensemble) structurally cannot.
- **Suggested TODO**: <none — file as the *field-level / conformalize-the-distance-field* arm of the σ-calibration-quality axis (Q-015) and the P3 obstacle-field calibration locus; when the P5 σ-calibration harness is specced, add a **field-level functional-conformal coverage** metric over the distance/obstacle field (not just scalar ECE/reliability) and evaluate the FCP envelope-as-tightened-sampler-constraint against the per-cell OCULAR [[OCULAR-conformal-dynamics-uncertainty]], global CSRC [[2606.04185]], and latent Pixels-to-Proofs [[2606.15594]] calibration loci — does calibrating the *geometry field the sampler queries* catch a collision miscalibration the dynamics-residual σ misses, and log the **AFCP online update** as an adaptation mechanism to compare vs the Bayesian-consistency shrink [[2511.21871]]; sampler-native (unlike the SLS/CBF conformal arms) so more portable to nav2_mppi, but it calibrates obstacle geometry, not the P2 ensemble σ — concept-only borrow, no code, no diff-drive.>

## 2026-07-04 04:00 — Biased Dreams: Limitations to Epistemic Uncertainty Quantification in Latent Space Models
- **Source**: https://arxiv.org/abs/2604.25416
- **Type**: arxiv
- **Why relevant**: Julia Berger, Bernd Frauenknecht, Sebastian Trimpe, Bastian Leibe (v1 Apr 28 2026; cs.LG; Dreamer/RSSM family, DMControl-flavored MBRL). **The *cautionary / null-result* arm of the σ-calibration-quality axis — the first feed entry that argues an epistemic σ read off a *learned latent world model can be systematically un-calibrated in exactly the region the project cares about (OOD / 미관측)*, which is the adversarial counterpoint the calibrated-latent optimism arms (ELVIS [[2605.04709]], Pixels-to-Proofs [[2606.15594]]) never confront.** Mechanism / finding: in latent-space world models, **latent transitions are biased toward well-represented regions of latent space — an "attractor" behavior** where the learned latent drifts back toward densely-trained states instead of tracking true dynamics; consequently **model↔environment discrepancies may never surface in the latent at all**, so any uncertainty signal computed *in that latent* (ensemble spread, disagreement, posterior variance) fails to fire where the model is actually wrong, and because the attractors sit in **high-reward regions, predicted returns are systematically over-estimated** (optimistic bias). This is a direct structural warning to three things the feed has been building toward: (1) the **ELVIS [[2605.04709]] ensemble-UCB-optimism mode** — if the ensemble's disagreement is measured in an attractor-biased latent, "explore toward the upper bound" explores toward a *phantom* optimism the latent manufactured, not a real epistemic gap; (2) the **Pixels-to-Proofs [[2606.15594]] conformalize-the-latent bet** — conformal calibration assumes the latent's error is *observable to calibrate against*, but if discrepancies never appear in the latent the calibration set is blind to them; (3) the **Bayesian-consistency shrink-guarantee [[2511.21871]]** — "does σ shrink correctly as the ensemble learns" presumes the ensemble *sees* its own error, which attractor bias can hide, so a σ can look consistently-shrinking while staying wrong. For the P5 σ-calibration-quality harness this reframes the core question from "is σ calibrated on held-out data" to **"is σ calibrated in the latent regions the encoder under-represents"** — i.e. a calibration metric must be *stratified by latent-space density*, not just aggregate ECE/coverage, or it will certify a channel that is over-confident precisely in the 미관측 tail. North-star fit: P3 (the epistemic channel's failure mode — a learned-latent σ can be blind to its own OOD error) + P2 (a warning on any learned-latent / world-model front-end, incl. the ELVIS/Pixels-to-Proofs Dreamer-style encoders) + P5 (motivates a *density-stratified* σ-calibration metric beyond aggregate ECE). Caveats: **pure cs.LG MBRL result on the Dreamer/RSSM family (image-observation DMControl-style), no robot / diff-drive / Nav2 / Gazebo, no MPPI, no code surfaced in the abstract; it is a diagnosis, not a fix** — it identifies attractor bias but does not hand you a calibrated estimator; whether the attractor pathology reproduces in the project's *planned BEV+residual* front-end (lighter than a full RSSM) is unvalidated and is itself worth checking. Borrow is strictly the **"latent attractor bias ⇒ epistemic σ blind in under-represented regions ⇒ optimistic-return bias" diagnosis as a red-team lens on the P3 epistemic channel and a design constraint on the P5 σ-calibration harness** — stratify any σ-calibration metric by latent-space density and explicitly probe the OOD/미관측 tail, and treat the ELVIS [[2605.04709]] ensemble-UCB and Pixels-to-Proofs [[2606.15594]] conformal-latent arms as *hypotheses to falsify* against this attractor failure mode rather than settled calibration recipes.
- **Suggested TODO**: <none — file as the *cautionary / attractor-bias* arm of the σ-calibration-quality axis; when the P5 σ-calibration harness is specced, add a **density-stratified calibration check** (score σ coverage/ECE *conditioned on latent-space representation density*, not just aggregate) and an explicit OOD/미관측-tail probe, and use the "latent attractor ⇒ σ blind in under-represented regions ⇒ optimistic-return bias" finding to red-team the ELVIS [[2605.04709]] ensemble-UCB-optimism and Pixels-to-Proofs [[2606.15594]] conformal-latent arms — do their calibration claims survive when the discrepancy never enters the latent? concept-only borrow, since it is a Dreamer/RSSM MBRL diagnosis with no nav / sampler / code.>

## 2026-07-03 20:00 — ELVIS: Ensemble-Calibrated Latent Imagination for Long-Horizon Visual MPC
- **Source**: https://arxiv.org/abs/2605.04709
- **Type**: arxiv
- **Why relevant**: Yurui Du, Pinhao Song, Yutong Hu, Renaud Detry (v1 May 7 2026; cs.LG/cs.RO/eess.SY; 14 DeepMind Control visual tasks + real-world sand-spraying). **The *sampler-native, ensemble-UCB* counterpart to Pixels-to-Proofs [[2606.15594]] on the P2→P3 calibration handoff — same learned-latent-world-model + calibrated-ensemble bet, but driven by a mode-keeping *sampler* and turning the ensemble into an *optimism* signal rather than a hard constraint.** Mechanism: a **Dreamer-style recurrent state-space model** gives a learned latent in which long-horizon rollouts are cheap; an **ensemble of latent critics defines an upper-confidence-bound (UCB) score** so the planner is optimistic under epistemic uncertainty; and a **Gaussian-mixture MPPI** carries the plan while **avoiding mode averaging** over the long horizon — reported SOTA vs **TD-MPC2 and DreamerV3**. For this project it is two arms at once. (1) The **P2 ensemble bottleneck**: where the discrete residual arms (PMC12788286 [[PMC12788286]], ICODE [[2605.03260]]) emit a residual and the RiskInflationCritic inflates a k·σ *margin* against the ensemble spread, ELVIS shows the ensemble σ can instead be an **upper bound to explore *toward*** — the optimism pole opposite the certainty-constraint anti-exploitation arm (UPSi [[2604.26836]]) and passive k·σ margin, and distinct from active perception (PA-MPPI [[2509.14978]]) and the conformal-latent-constraint (Pixels-to-Proofs [[2606.15594]]): five epistemic-response modes now — passive margin / active perception / certainty-constraint / conformal-constraint / **UCB-optimism**. (2) The **mode-keeping-sampler thread**: Gaussian-mixture MPPI is exactly the multi-modal proposal SVG-MPPI / SOPPI [[2511.02015]] / OT-MPC [[2605.02147]] argue for, here motivated by long-horizon latent rollouts where a unimodal proposal averages across basins. North-star fit: P2 (calibrated-ensemble latent front-end) + P3 (the ensemble σ as an epistemic-response knob) + the sampler-core mode-keeping thread. Caveats: **DMControl visual RL + manipulation-flavored sand-spraying — no diff-drive / Nav2 / Gazebo / crowd, so sim-to-nav transfer is unvalidated**; a **Dreamer-style latent world model is a heavier front-end than the planned BEV+residual stack** (adopting it is a representation-architecture decision, like Pixels-to-Proofs, not a critic add-on); the ensemble "calibration" is asserted but the calibration method isn't pinned in the abstract (UCB from an ensemble of critics); **no code surfaced**. Borrow is strictly the **ensemble-of-latent-critics UCB as an *optimism* epistemic-response mode (explore toward the ensemble's upper bound) + Gaussian-mixture MPPI as the mode-keeping sampler that carries it** — file against the passive-margin / active-perception / certainty-constraint / conformal modes and ask whether an optimism-under-epistemic-uncertainty sample ever *helps* nav (finding the gap in a partially-observed cafe) rather than only inviting exploitation.
- **Suggested TODO**: <none — file as the *ensemble-UCB-optimism + Gaussian-mixture-MPPI* arm of the P2 ensemble bottleneck and the epistemic-response taxonomy; when the P2 ensemble σ interface and the epistemic-response modes are specced, add "explore toward the ensemble UCB (optimism)" as a fifth mode alongside passive k·σ margin (RiskInflationCritic), active perception (PA-MPPI [[2509.14978]]), certainty-constraint (UPSi [[2604.26836]]) and conformal-latent-constraint (Pixels-to-Proofs [[2606.15594]]), and note Gaussian-mixture MPPI as the mode-keeping sampler that carries it (SOPPI [[2511.02015]] / OT-MPC [[2605.02147]]); concept-only borrow — Dreamer-style latent front-end is a representation-architecture decision, DMControl is not nav.>

## 2026-07-03 20:00 — Deep Probabilistic Traversability with Test-Time Adaptation for Uncertainty-Aware Rover Navigation
- **Source**: https://www.nature.com/articles/s41598-026-40109-1
- **Type**: arxiv
- **Why relevant**: Masafumi Endo, Tatsuya Taniai, Genya Ishigami (Scientific Reports, 2026; planetary rover, natural terrain). **The *traversability channel* arm the P3 roadmap names but the feed is thin on — almost every recent P3 entry calibrates a *dynamics* residual or a *pedestrian* forecast; this one is squarely the terrain/traversability channel, and it is the feed's first entry that adapts that channel *at test time* for the 미관측 distribution.** Mechanism: an **end-to-end probabilistic model maps terrain appearance + geometry → a slip *distribution*** (not a point estimate), **decomposes epistemic vs aleatoric** uncertainty in the slip prediction, and feeds that uncertainty back as an **uncertainty-aware traversability cost** in path planning; crucially it supports **test-time adaptation** — the pre-trained model is refined online from **in-situ traverse experience** to cut prediction error in a new environment. The TTA loop is the direct answer to the project's 미관측-distribution (real-world rosbag) clause: the traversability channel should not be frozen at training time but should adapt as the robot experiences the actual venue. It slots into the P3 risk field as the *terrain* channel feeding the same aleatoric+epistemic split the calibration thread tracks (OCULAR [[OCULAR-conformal-dynamics-uncertainty]], the Bayesian-ambiguity/consistency arm [[2511.21871]]) — but keyed on **terrain**, not dynamics or agents, and it is the only arm so far that *learns online in the deployment venue*. North-star fit: P3 (traversability channel + epistemic/aleatoric decomposition) + the 미관측-rosbag distribution-shift stage (test-time adaptation) + P5 (slip-prediction calibration and the adaptation gain are scoreable metrics). Caveats: **planetary rover / wheel-slip on natural terrain — no indoor cafe, no Nav2 / Gazebo diff-drive; the *slip* observable maps awkwardly to a flat indoor floor** (there traversability is more clutter/surface than wheel slip), so the transfer is the *predict-a-distribution + decompose-uncertainty + adapt-at-test-time* recipe, not the slip model itself; **planner is a rover global planner, not MPPI** (a cost consumer — port the traversability cost into a critic); **Scientific Reports** (open-access, but no arxiv mirror surfaced); **code-release status unconfirmed** (check on read). Borrow is the **distributional traversability + epistemic/aleatoric decomposition + test-time adaptation as the P3 traversability-channel recipe**, especially the TTA loop for the 미관측-rosbag stage — evaluate it against a frozen-at-training traversability cost on a venue-shift scene, scoring whether online adaptation cuts traversability-cost miscalibration where a static channel drifts.
- **Suggested TODO**: <none — file as the *traversability-channel + test-time-adaptation* arm of the P3 risk field; when the P3 traversability channel is specced, adopt the distributional-slip / epistemic-aleatoric-decomposition / test-time-adaptation recipe as the terrain-channel design and use the TTA loop as the 미관측-rosbag distribution-shift adapter, scored against a frozen traversability cost; concept-only borrow — the planetary-rover wheel-slip model does not map directly to indoor flat-floor nav, and the planner is a rover global planner, not a sampler.>

## 2026-07-03 16:00 — Pixels to Proofs: Probabilistically-Safe Latent World Model Control via Parallel Conformal Robust MPC
- **Source**: https://arxiv.org/abs/2606.15594
- **Type**: arxiv
- **Why relevant**: Devesh Nath, Anutam Srinivasan, Haoran Yin, Ruitong Jiang, Jeffrey Fang, Glen Chou (v1 Jun 14 2026; cs.RO). **The *representation-native* calibration arm the feed's guarantee thread has circled but never landed — it is the first entry that conformally calibrates the *learned latent world model's own error* and turns that into a robust constraint set, closing the exact "pixels → proofs" loop the project's core hypothesis names (representation quality *is* the ceiling on control quality, so calibrate the representation, not just the dynamics residual around it).** Mechanism (SLS²): an **action-conditioned joint-embedding world model with Markovian latent states** is paired with **GPU-accelerated System Level Synthesis (SLS) robust MPC** and a **conformalized latent constraint checker** — conformal prediction yields *calibrated latent error bounds*, those bounds define *robust latent-space constraint sets*, and SLS does efficient gradient-based trajectory optimization through the learned latent dynamics while respecting them, giving probabilistic safety from raw pixels against both model imperfection and OOD inputs. This is structurally distinct from every calibration arm already in the feed: OCULAR [[OCULAR-conformal-dynamics-uncertainty]] conformalizes the *dynamics model's* uncertainty per-cell from semantic images (calibration in state-space, keyed on perception); CSRC [[2606.04185]] gives a global distribution-free conformal coverage set; DRA-MPPI [[2506.21205]] conformalizes *rollout selection*; the prediction-side arms ([[2603.10407]], [[2512.05682]]) conformalize an *other-agent forecast* — Pixels-to-Proofs is the only one that calibrates the **representation itself** (the latent embedding's prediction error) and manufactures the constraint set *in latent space*, which is precisely the P2→P3 handoff the project's BEV/representation front-end will need: the learned encoder emits a latent, and the question this branch keeps hitting is "how much do we trust a plan evaluated *in that latent* — is the representation's error calibrated *here*." It also sharpens the epistemic-response taxonomy: alongside passive k·σ margin (RiskInflationCritic), active perception (PA-MPPI [[2509.14978]]), certainty-constrained anti-exploitation (UPSi [[2604.26836]]), and the contraction-tube [[2507.02098]], this is the **"calibrate the latent and make its error bound a hard robust constraint set"** mode — the certificate applied to the representation, not the residual. North-star fit: **the core-hypothesis loop (representation → provable control): P2 (a learned-world-model / latent front-end whose error is conformally calibrated) + P3 (a robust, coverage-guaranteed constraint set built from the representation's own uncertainty) + P5 (latent-error coverage + the SLS constraint-tightening level are guarantee-bearing knobs the harness can sweep, and the OOD-robustness claim is a 미관측-rosbag distribution-shift test)**. Caveats: **vision-based control tasks, platform/robot/simulator unspecified in the abstract — no diff-drive / Nav2 / Gazebo confirmed, and it is optimization-based SLS robust MPC with gradient-based trajectory optimization through latent dynamics, NOT a sampler** (the borrow is the *conformalized-latent-constraint-set-from-a-learned-world-model concept*, not the SLS-QP machinery, which does not drop into gradient-free nav2_mppi); the **joint-embedding world model is a heavier front-end than the project's planned BEV-plus-residual stack** (adopting it is a representation-architecture decision, not a critic add-on); **no code surfaced; no diff-drive/crowd numbers; the epistemic/aleatoric split is not explicitly decomposed** (conformal bounds absorb both). Borrow is strictly the **conformal calibration of the learned latent representation's prediction error into a robust latent-space constraint set** — when the P2 representation front-end emits a latent (BEV embedding or world-model state), conformalize *its* error and feed a coverage-guaranteed constraint set to the P3 risk field, evaluated against the state-space-local OCULAR [[OCULAR-conformal-dynamics-uncertainty]] calibration and the global CSRC [[2606.04185]] set on a scene with representation error in an OOD/미관측 region, asking whether calibrating *the representation itself* catches a failure that calibrating only the dynamics residual around a trusted representation cannot.
- **Suggested TODO**: <none — file as the *representation-native / conformalize-the-latent* arm of the P3 calibration thread and the P2→P3 handoff; when the P2 BEV/world-model front-end is specced, evaluate conformalizing the *latent representation's own* prediction error into a robust constraint set (Pixels-to-Proofs SLS² style) as a distinct calibration locus vs the state-space-local OCULAR [[OCULAR-conformal-dynamics-uncertainty]] and global CSRC [[2606.04185]] arms — does calibrating the representation catch an OOD/미관측 failure that calibrating only the residual around a trusted latent misses; concept-only borrow since it is optimization-based SLS robust MPC over a joint-embedding world model, not a sampler, and adopting the world-model front-end is a representation-architecture decision, not a nav2_mppi critic add-on.>

## 2026-07-03 12:00 — DR-ACBF: Distributionally Robust Acceleration Control Barrier Filter for UAV Obstacle Avoidance
- **Source**: https://arxiv.org/abs/2512.00462
- **Type**: arxiv
- **Why relevant**: Dnyandeep Mandaokar, Bernhard Rinner (v1 Nov 29 2025, **v2 Mar 26 2026**; eess.SY; Crazyflie hardware). **The *lightweight, constant-time* pole of the chance-constrained-collision thread — it is the cheapest per-obstacle risk-tightening kernel the feed has logged, the compute-budget counterpoint to GLR's [[2507.18819]] Gauss–Legendre cubature integral.** Mechanism: a **second-order control barrier function rendered as linear half-space constraints on commanded acceleration**, with **Cantelli (one-sided Chebyshev) tightening per-obstacle** plus a **DR-CVaR early-trigger** to fire avoidance under distributional uncertainty, all solved by **constant-time Gauss–Southwell coordinate projections** — reported "similar avoidance performance at substantially lower computational effort than SOTA" on Crazyflie drones. The value for the project is not the drone or the CBF-QP but the **collision-chance kernel**: where GLR [[2507.18819]] buys a bounded-error probability via cubature over the swept geometry (accurate, ~1000 Hz but integral-heavy), DR-ACBF buys a **distribution-free Cantelli bound per obstacle** — a closed-form, moment-based tightening that needs only the obstacle's mean+variance, no Gaussian assumption and no quadrature — which is exactly the kind of O(1)-per-obstacle term a gradient-free sampler like nav2_mppi can afford to evaluate on every rollout against every obstacle inside the DM-MPPI [[2512.00759]] pruning budget. It slots alongside the idx-4 aleatoric cost-critic arms as a *third* chance-constraint kernel option: GLR [[2507.18819]] (cubature, exact-ish, expensive), the RL-CVaR-barrier [[2605.21257]] (learned α, RL-trained), and now Cantelli/DR-CVaR (moment-based, distribution-free, cheap). North-star fit: object-avoidance completeness (static + dynamic obstacles, the DR-CVaR early-trigger handles the moving case) + the aleatoric-risk axis + the CPU-budget concern the feed keeps pricing. Caveats: **UAV / Crazyflie / eess.SY — no diff-drive, no Nav2, no Gazebo, and it is a CBF-QP acceleration filter, NOT a sampler** (the borrow is strictly the *Cantelli-per-obstacle + DR-CVaR-early-trigger chance-constraint kernel*, not the half-space-QP machinery, which does not drop into gradient-free MPPI); the acceleration-command / second-order-integrator framing must be mapped onto a diff-drive velocity-command model; **no code surfaced; the "substantially lower compute" claim is vs CBF baselines, not vs MPPI cost terms**; Cantelli is a loose (worst-case one-sided) bound, so it may over-tighten vs GLR's tighter integral in easy scenes. Borrow is strictly the **moment-based, distribution-free per-obstacle Cantelli tightening (+ DR-CVaR early-trigger for dynamic obstacles) as a cheap O(1)-per-obstacle collision-chance kernel** for the idx-4 aleatoric cost-critic — benchmark its conservatism vs compute against the GLR [[2507.18819]] cubature kernel on cafe-narrow, asking whether a closed-form moment bound is "good enough" to feed the sampler's chance term at a fraction of GLR's per-rollout cost.
- **Suggested TODO**: <none — file as the *lightweight, moment-based* chance-constraint-kernel arm of the idx-4 aleatoric cost-critic thread; when the aleatoric collision-probability interface is specced, add **Cantelli per-obstacle tightening (+ DR-CVaR early-trigger)** as a third kernel option — the cheap, distribution-free, O(1)-per-obstacle counterpoint to the GLR [[2507.18819]] cubature integral and the RL-learned CVaR level [[2605.21257]] — and score its conservatism-vs-compute tradeoff, concept-only borrow since it is a UAV CBF-QP filter, not a sampler.>

## 2026-07-03 08:00 — Bayesian Risk-averse MPC with Consistency and Stability Guarantees
- **Source**: https://arxiv.org/abs/2511.21871
- **Type**: arxiv
- **Why relevant**: Yingke Li, Yifan Lin, Enlu Zhou, Fumin Zhang (v1 Nov 26 2025; math.OC). **The *Bayesian-ambiguity-set* arm of the risk-calibration thread — and the one entry that gives the σ-calibration-quality branch a principled account of *what a well-calibrated epistemic channel should do over time*: the ambiguity set should shrink as the model learns, and there should be a consistency theorem saying it converges to the truth.** Mechanism: a risk-averse MPC for nonlinear stochastic systems that **explicitly separates epistemic from aleatoric uncertainty** — parametric (epistemic) uncertainty is handled by **dynamically-shrinking ambiguity sets built from Bayesian credible intervals, approximated online with a particle filter**, while disturbances (aleatoric) are treated risk-neutrally; it proves **Bayesian consistency** (the credible-interval ambiguity set contracts to the true parameter as data accrues) and **risk-averse asymptotic stability** via a Lyapunov argument, and gives both an optimal and a warm-started sub-optimal receding-horizon policy for real-time use. This is the distributionally-robust cousin of the DRO-radius arm [[2603.04914]] but with the radius *derived from a Bayesian posterior and provably shrinking* rather than fixed/hand-set — which is exactly the question the σ-calibration-quality axis is built around: a calibrated epistemic σ is not just "right on average now" but "tightens correctly as the P2 ensemble sees more of a region." It also formalizes the epistemic/aleatoric split the feed tracks empirically — TRIAGE [[2512.14331? see feed]] type-routes aleatoric vs epistemic response, LR-MPC [[2506.14305]] adapts a worst-case-CVaR level online, RAAR-MPC [[2507.11420]] closes a loop on realized violation rate, OCULAR [[OCULAR-conformal-dynamics-uncertainty]] calibrates per-cell from perception — this paper is the only one that gives a **convergence/consistency guarantee on the epistemic ambiguity set itself**, the theoretical backstop for "does our calibration actually get better as the model learns, or just look calibrated?" North-star fit: P3 (the epistemic channel's calibration-over-time semantics — a shrinking, consistency-guaranteed ambiguity set) + P2 (the parametric-uncertainty-from-a-learned-model framing matches the residual-ensemble posterior) + P5 (Bayesian consistency + the shrink-rate are *guarantee-bearing calibration-quality metrics* the harness can score, distinct from static ECE/coverage). Caveats: **math.OC theory paper — no robot / diff-drive / Nav2 / Gazebo, no experiments, no code; optimization-based receding-horizon, not a sampler** (the borrow is the *ambiguity-set-from-Bayesian-credible-interval + consistency* concept, not the QP machinery — it does not drop into a gradient-free MPPI); particle-filter parameter inference is a per-step cost against the DM-MPPI [[2512.00759]] pruning budget; the linear-in-parameters / nonlinear-stochastic-system framing must be mapped onto our nonlinear residual+ensemble before the consistency proof transfers; **no quantitative results at all — it is a foundations paper**. Borrow is strictly the **Bayesian-credible-interval ambiguity set with a provable shrink/consistency guarantee as the calibration-over-time model for the P3 epistemic σ channel** — score the ambiguity set's *shrink rate* and *coverage-as-it-learns* as a P5 σ-calibration-quality metric (not just static ECE), contrasted with the fixed DRO radius [[2603.04914]], the online-adapted CVaR level (LR-MPC [[2506.14305]]), and the violation-rate loop (RAAR-MPC [[2507.11420]]).
- **Suggested TODO**: <none — file as the *Bayesian-ambiguity-set / consistency-guarantee* arm of the σ-calibration-quality axis; when the P5 σ-calibration-quality harness is specced, add a **"does σ shrink correctly as the P2 ensemble learns"** metric (ambiguity-set shrink rate + coverage-as-data-accrues, backed by the Bayesian-consistency notion) alongside static ECE/reliability, and log the Bayesian-credible-interval ambiguity set as a third radius-source option vs the fixed DRO radius [[2603.04914]] and the online-CVaR-level adapters (LR-MPC [[2506.14305]] / RAAR-MPC [[2507.11420]]) — concept-only borrow, since it is optimization-based math.OC, not a sampler.>

## 2026-07-02 16:00 — Rethinking Gaussian Trajectory Predictors: Calibrated Uncertainty for Safe Planning
- **Source**: https://arxiv.org/abs/2603.10407
- **Type**: arxiv
- **Why relevant**: Fatemeh Cheraghi Pouria, Mahsa Golchoubian, Katherine Driggs-Campbell (Mar 11 2026; cs.RO; real-world pedestrian scenarios + uncertainty-aware MPC). **The *predictor-side calibration* arm of the crowd-navigation thread — it attacks a failure the feed's prediction-quality entries only diagnosed: a Gaussian pedestrian predictor's reported confidence is usually *mis-calibrated*, so an MPC that trusts its covariance is over/under-confident regardless of how accurate the mean is.** Mechanism: a **calibration loss** that uses **Kernel Density Estimation to estimate the empirical distribution of confidence levels** and forces it to match the **Chi-squared distribution** implied under the Gaussian assumption (with an MSE term to keep the mean accurate), then feeds the *recalibrated* Gaussian into an **uncertainty-aware MPC** for "collision-free navigation in complex scenarios" on real pedestrian data. This is distinct from the feed's other uncertainty arms: "How Human Motion Prediction Quality Shapes..." [[human-motion-prediction-quality-social-nav]] measured that better prediction → better nav but left *calibration* untouched; OCULAR [[OCULAR-conformal-dynamics-uncertainty]] conformally calibrates *dynamics-model* uncertainty from images, not the *pedestrian predictor's* confidence; DRA-MPPI [[2506.21205]] does conformal coverage over rollouts downstream. Rethinking-Gaussian instead fixes the predictor's *own* confidence before the planner ever sees it — the "make the covariance you feed the chance-constraint actually mean 68%" knob, and a natural upstream companion to the dynamic-risk cost arms. North-star fit: P4 (dynamic 동적 장애물, pedestrian 회피) + P3 aleatoric channel (calibrated per-agent covariance) + P5 (calibration quality is directly a metric the risk-calibration harness should score). Caveats: needs a Gaussian-output predictor (doesn't cover multimodal-mixture heads directly); MPC not MPPI (a cost/constraint consumer — port the calibrated covariance into the sampler's chance term); no quantitative collision/success numbers in the abstract; no code surfaced.
- **Suggested TODO**: when building the P5 risk-calibration harness's aleatoric arm, add a **predictor-calibration stage** (KDE-of-confidence → Chi-squared matching loss) upstream of the pedestrian-covariance chance term so the covariance nav2_mppi consumes is actually calibrated — score calibration (reliability diagram / ECE) as its own metric alongside collision/near-miss, and compare feeding *raw* vs *recalibrated* Gaussian covariance into the dynamic-risk cost.
